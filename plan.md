# План: Лекало — площадка поиска торгов

> Статус и дорожная карта. Операционка (как собрать/задеплоить, эндпоинты, грабли) —
> в [`CLAUDE.md`](CLAUDE.md). Версия 2.0 · обновлено 2026-07-28.
> Легенда: `[x]` сделано · `[~]` частично · `[ ]` нет · ⭐ приоритет.

## 1. Что это

Собственная площадка поиска госзакупок (44/223-ФЗ) — аналог Тендерплана/Контура, но
**независимая**: свой сбор данных напрямую с площадок. Поиск по ключевым словам, лента
закупок со всех источников, фильтры, скачивание документов, личные кабинеты компаний.
Дифференциатор сверху — **сверка своего ТЗ с ТЗ закупки** (надстройка).

**Разворот (было→стало):** проект начинался как «SpecMatch» — надстройка на API
Тендерплана (подбор закупок под товар по характеристикам ТЗ). Ушли от зависимости от
конкурента к своей площадке со своим сбором данных.

## 2. Где мы сейчас — РАБОТАЕТ И ЗАДЕПЛОЕНО

Живой сайт: **https://186.246.30.213.nip.io/** (HTTPS, Let's Encrypt; голый IP
`http://186.246.30.213/` тоже работает для ленты/админки — вход/кабинет с него
редиректят на HTTPS-домен, т.к. secure-cookie по HTTP браузер не сохранит).

### Витрина и данные
- [x] Фронт-витрина: поиск по ключевым словам, лента карточек, **постраничная
  навигация** (номера страниц, выбор 10/30 на странице), «просмотренные» тускнеют.
- [x] **Поиск переработан на точность**: свой стемминг (список типичных русских
  окончаний, не блинд «−2 буквы»), совпадение **по началу слова** (не по любому месту
  в строке — раньше «IT» ловило «Security»/«City»), **все слова запроса обязательны**
  (не «любое из», иначе «строительные материалы» ловил и горюче-смазочные), ОКПД2
  исключён из текстового поиска (это классификатор, а не то, что реально покупают).
- [x] Фильтры: заказчик, регион, срок поставки, сумма (НМЦК), закон (44/223), этап,
  площадка. Регион — из реальных данных **у обоих источников** (ЕИС тоже, порт
  на общую базу, см. §3).
- [x] Карточка: позиции, сроки, обеспечение, **скачивание документов** (прямые ссылки).
- [x] **Живое устаревание**: просроченные по сроку подачи уходят из ленты сами (+ раз в минуту).
- [x] Два источника, единая лента: **Портал поставщиков (mos.ru)** + **ЕИС (zakupki.gov.ru)**.
- [x] **~4000+ закупок** в снапшоте (Портал ~500 + ЕИС ~3700), сбор ~5-6 мин.
  ЕИС теперь включает и общий список (curl, ~2000), и **прицельный поиск по ~30
  темам** (медицина, стройка, IT, услуги и т.д. — см. §3 про антибот).
- [x] Автообновление: Планировщик Windows раз в час → пересбор → деплой при смене состава.
  Проверено: интервал ровно `PT1H`, 0 пропусков за 110+ запусков подряд.
- [x] Сборщики за единым интерфейсом (`tools/sources/*`) — новая площадка = новый модуль.
- [x] Сетевой слой (`tools/sources/util.js`) — повтор при сбоях (2 попытки), реальные
  сообщения об ошибках curl (была история: один упавший запрос ронял источник целиком
  без объяснения причины).

### Личные кабинеты (реализовано полностью)
- [x] Регистрация/вход компаний, bcrypt-хеш паролей, httponly+secure session-cookie
  (30 дней). SQLite на VPS (`server/data/lekalo.db`, не в git).
- [x] У каждой компании и каждого пользователя свой номер (`id`), видно в кабинете и в админке.
- [x] Сотрудники: владелец добавляет с отдельным email/паролем, свой вход, свои данные.
  Сохранённые закупки и история сверок ТЗ — **лично у каждого** (`user_id`), не общие
  на компанию.
- [x] Личный кабинет (`account.html`): данные компании (редактирование), подписка +
  дата окончания, список/добавление/удаление сотрудников, история сверок ТЗ.
- [x] Админ-панель (`/api/admin`, HTTP Basic) — список всех зарегистрированных компаний,
  для владельца сайта.
- [x] ИНН уникален на компанию (нельзя зарегистрировать/сменить на уже занятый).
- [x] **Rate-limit на вход/регистрацию** (P0-безопасность): гвард по IP и по email
  до bcrypt (режет и брутфорс, и CPU-DoS через дорогой хэш), успешный вход обнуляет счётчик.
- [x] CORS сужен с `*` до конкретного HTTPS-домена, `allow_credentials=False` явно.
- [x] HSTS на HTTPS-домене.

### ТЗ-сверка (дифференциатор)
- [x] Клиентский парсинг `.docx`/`.txt` прямо в браузере (`tzmatch.js`) — без бэкенда.
  Разбор ZIP переписан на чтение официального центрального каталога (был хрупкий
  побайтовый поиск локальных заголовков, мог портить файлы с потоковой записью ZIP).
- [x] Серверный движок (`server/app/matching.py`) — тот же алгоритм, порт для будущего
  живого API; на ошибке разбора отдаёт пустую строку (не мусор из бинарных байт).
- [x] Результат сверки автоматически уходит в личную историю (см. кабинет).
- [ ] LLM-извлечение требований (Sonnet) вместо эвристики — не начато.

## 3. ⭐ Открытое прямо сейчас

1. **Антибот источника глушит поиск по словам для «голых» HTTP-клиентов.** Обнаружено
   2026-07-28: curl с этой машины получает «0 записей» на ЛЮБОЙ поисковый запрос
   (проверено заведомо новыми словами), а тот же запрос через настоящий headless-Chromium
   (Playwright) с той же машины/IP в тот же момент — реальные результаты. Не IP-бан,
   а разбор клиента (TLS/JS-отпечаток). **Решение уже внедрено**: прицельный поиск по
   темам (`collectEis`) теперь идёт через Playwright вместо curl (общий список — по-прежнему
   curl, там антибот не мешает). Плюс пауза 400-800мс между темами и честные
   Accept/Accept-Language заголовки — чтобы не выглядеть как обстрел. **Не пытаться
   обходить проксями/спуфингом** — это уже не инженерия, а обход защиты чужого сайта.
2. **Git-рост снапшота** — снапшот сильно потяжелел (было 2.1 МБ/~1000 закупок, теперь
   на порядок больше при ~4000+ закупках), коммитится ежечасно. Решить: оставить,
   ограничить объём, или доставлять на VPS через `scp` вместо git.
3. **2FA** — не сделано (пароли + rate-limit есть, двухфакторки нет).

## 4. Дорожная карта

### Данные и охват
- [x] Регион ЕИС.
- [~] Больше площадок за тем же интерфейсом. **Разведка (2026-07-23):**
  - **РТС / Фабрикант / Газпром — НЕ берутся автосбором.** Даже настоящим headless-Chromium
    (Playwright) реальные домены РТС (`rts-tender.ru`, `market.rts-tender.ru`, `223.rts-tender.ru`)
    отдают `ERR_CONNECTION_CLOSED`/`ERR_TIMED_OUT`; `etpgpb.ru` не пускает; Фабрикант нестабилен/000.
    Блок на уровне соединения/сети, не JS-челлендж. **NB:** `rts-market.ru` — чужой припаркованный
    домен (Авиасейлс), не площадка РТС. Их регулируемые 44/223 всё равно есть в ЕИС.
  - Реальные пути к ним: официальный API/аккредитация у площадки (не код). ТЭК-Торг — не проверяли.
- [x] Больше объёма — ЕИС общий список 900→2000, Портал 400→500, + 30 тем прицельного поиска.

### Полный реестр вживую (следующий уровень архитектуры)
- [ ] **БД-бэкенд** (FastAPI + Postgres) на хостинге **с доступом к источникам** (текущий VPS
  заблокирован гос-сайтами для СБОРА, хотя для личных кабинетов/API аккаунтов работает
  нормально — это разные вещи). Тогда: весь реестр (десятки тыс.), серверный поиск/фильтры,
  пагинация по API, документы/сверка по запросу.

### Продукт / прод
- [x] Домен + HTTPS (Let's Encrypt через nip.io).
- [x] Личные кабинеты, регистрация компаний, сотрудники.
- [ ] Сохранённые поиски на сервере (сейчас всё ещё localStorage — только «Мои
  конкурсы»/сверка ТЗ переехали на сервер, сохранённые ПОИСКИ по ключевым словам — нет).
- [ ] Уведомления (Telegram/e-mail) о новых подходящих закупках.
- [ ] 2FA.

## 5. Зафиксированные решения

- **Источник — свой сбор напрямую** (не агрегатор). Портал поставщиков (чистый JSON) и ЕИС
  (HTML) вскрыты и работают анонимно.
- **Сбор — на машине пользователя**, т.к. гос-сайты блокируют IP дата-центров (VPS не видит источники).
- **Прод — статический снапшот на VPS** для ленты закупок (быстро, дёшево); **живой бэкенд
  для личных кабинетов** (аккаунты/сотрудники/подписка/сверка ТЗ — это не требует доступа
  к гос-источникам, поэтому работает на текущем VPS без ограничений).
- **Прицельный поиск по темам ЕИС — через headless-браузер (Playwright), не curl** — источник
  различает голые HTTP-клиенты и настоящие браузеры именно для поисковых запросов.
- **Скачивание файлов — прямыми ссылками** из браузера пользователя (CORS не мешает навигации).
- **Nexara — отдельный проект**, его заменили на этом VPS (бэкап снят).

## 6. Открытые вопросы

- Оставлять снапшот в git или доставлять `scp`-ом (растёт быстрее, чем раньше)?
- Приоритет: сохранённые поиски на сервер, уведомления, или новые площадки?
- Свой домен вместо `nip.io` — когда?
