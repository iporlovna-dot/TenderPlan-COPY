# План: SpecMatch — подбор закупок под товар по характеристикам ТЗ

> Подбор госзакупок (44-ФЗ / 223-ФЗ) под номенклатуру поставщика по **фактическим
> характеристикам из ТЗ**, а не по классификаторам ОКПД2/КТРУ.
> Версия документа: 0.4 · Обновлён: 2026-07-22
>
> **Документ из двух частей:** Часть I — что делаем (действия, статус, приоритеты).
> Часть II — как устроено (справка: архитектура, модели данных, стек). Легенда чек-листа:
> `[x]` сделано и проверено · `[~]` в работе / ждёт ключ · `[ ]` ещё нет · ⭐ приоритет сейчас.

---

---

# ЧАСТЬ I. ДЕЙСТВИЯ И ПРИОРИТЕТЫ

## 1. Где мы сейчас

**Этап 0 — ЗАКРЫТ (точность 100% на 20 golden, гейт ≥90% пройден). Этап 1 (MVP) — ИДЁТ.**
Сквозной конвейер на реальных данных: id/файл ТЗ → парсинг (`.docx/.pdf/.doc/.xls/.txt`+OCR) →
извлечение (Claude) → детерм. align (морфология+словарь, LLM только на остатке) → матчер → вердикт.

**Итог сессии 2026-07-27 (кратко; детали — в журнале ниже):**
- Новый товар/категория: **видеоларингоскоп BESDATA BD-DF**, карточка обкатана на **6 живых тендерах**
  (Курган/Омск/Барнаул×2/Новомосковск/Саратов), везде 0 нарушений.
- **8 системных фиксов движка** (align type-guard+remapped→gap, разрешение×2, конверсия единиц,
  булево-наличие, набор-SET, any-match списка, align max_tokens; детерм. слой align).
- **Golden 15→20**, точность замерена `scripts/measure_accuracy.py` = **100% (20/20)**.
- **Сквозной автономный цикл доказан живьём** (`run_auto` сам нашёл тендер) + `scripts/worker.py`
  (беспилотное обновление ленты) + `scripts/leads.py` + `scripts/seed_demo.py`.
- **MVP-бэкенд `api/`** (FastAPI, argon2+JWT+rate-limit, изоляция company_id, CRUD, лента) — tests 16/16.
- **Веб-кабинет `web/index.html`** (тёмный «сигнальный терминал») — вход→товары→лента карточками.
- **`.xls`-парсинг** (xlrd) — реальный НМЦК.xls читается.
- ⚠ Блокер: квота Anthropic исчерпана до 2026-08-01 → живое извлечение на паузе (ядро/матчер/эмбеддинги
  работают без ключа). 🚩 Пре-прод гейт специфичности — ЗАКРЫТ 2026-07-28 (см. Этап 0).
- **Сессия 2026-07-28:** align_keys на локальных эмбеддингах (bge-m3, порог 0.85, §3.7); пре-прод гейт
  закрыт (+3 негатива, специфичность 4/4=100%); align_values — эмбеддинги НЕ годятся (антонимы, §3.7);
  семантический отсев Шага 3 — модуль+тесты (`src/semantic_filter.py`), но калибровка на ЖИВОМ фиде
  показала: drop-фильтр на generic-предметах РФ не разделяет (перекрытие) → в воронку НЕ встраиваем,
  точный сигнал остаётся КТРУ/слова; майнер синонимов (`synonym_miner.py`, +4 группы в профиль);
  ДОЗАПОЛНЕНИЕ ПРОБЕЛОВ — бэкенд готов (`src/gapfill.py` + API detail/fill, §Этап1); КАРТОЧКА
  КОНТРАКТА — полная обвязка в API (`ContractCard`: даты торгов/итогов, обеспечения, аванс, §Этап1) +
  срок исполнения/этапы детерм. парсером (`contract_terms.py`); расчёт масштабирования (§15.1);
  разведка `zakupki.mos.ru` — это ГОСТ-крипто, не CA-бандл (отдельный инфра-эпик, отложено, §6);
  версионность закупки — детект+классификация formal/material (`versioning.py`, §6);
  2 новых скилла (plan-sync, embed-calibrate).
- Тесты зелёные: matcher 39/39, keymatch 37/37, golden 46/46 (23 фикстуры), semantic_filter 12/12,
  synonym_miner 7/7, gapfill 13/13, contract_terms 14/14, versioning 12/12, api 35/35, +parser/ktru/filter/lot.

### Журнал: сессия 2026-07-27
- **MVP-БЭКЕНД FastAPI — КАРКАС ГОТОВ (`api/`).** Secure-by-design аутентификация (argon2id, JWT из
  env-секрета, rate-limit логина с временной блокировкой, единый ответ, constant-time), мультиарендность
  (изоляция по `company_id` — данные компании не видны другим), CRUD товаров, лента
  `GET /products/{id}/leads` (ранжир + обвязка закупки), мост `api/ingest.py` (вердикты run_auto → БД).
  SQLAlchemy+SQLite, `tests/test_api.py` 16/16, живой uvicorn-smoke прошёл (health/register/routes).
  Архитектура: проверенный CLI-конвейер пишет вердикты → ingest → API отдаёт ленту компании; ядро (src/)
  не тронуто. Осталось: React-кабинет, cron-воркер, 2FA Telegram, миграции/Postgres.
- **СКВОЗНОЙ АВТОНОМНЫЙ ЦИКЛ ДОКАЗАН НА ЖИВЫХ ДАННЫХ (продуктовый цикл).** `run_auto.py` (фид →
  воронка КТРУ/слова/срок/регион → скачивание ТЗ → извлечение → align → match) прогнан для BESDATA
  БЕЗ ручных id: система сама нашла 3 закупки — «принадлежности» 0% и «клинки» 13% КОРРЕКТНО
  отклонены (чужое), а видеоларингоскоп 6a6372cf (КТРУ точная ...7689, ГУЗ КБ СМП №7, НМЦК 489 510 ₽,
  8 дн до подачи) найден и принят 50% ПОДХОДИТ. Дискриминация на НЕЗНАКОМЫХ живых данных подтверждена.
  Новое: вердикт run_auto обогащён обвязкой закупки (заказчик/НМЦК/срок/регион/ссылка); `scripts/leads.py`
  — ранжированная ЛЕНТА подходящих закупок (продуктовый вывод = «карточка контракта в ленте», Этап 1).
- **Детерминированный слой align_keys (первый кирпич самообучения) СДЕЛАНО.** Перед LLM `align_keys`
  сводит имена детерминированно: (1) морфология (`_kname`: ё/е, порядок токенов, разделители,
  единицы-суффиксы) — «объем_памяти»≡«объём_памяти_гб»; (2) словарь `key_synonyms` в профиле (группы
  эквивалентных ИМЁН полей) — «разрешение_видеокамеры»↔«разрешение_камеры», «угол_обзора»↔… LLM
  вызывается ТОЛЬКО на нераспознанном остатке (дешевле + меньше обрывов + СТАБИЛЬНО). Это и есть
  фундамент самообучения: словарь копится из подтверждённых пар. keymatch 31/31 (+6). ЖИВОЙ ЭФФЕКТ:
  АКОД 81→97, НГКБ 85→89, Саратов 74→78 (и меньше разброса между прогонами — часть полей ушла из-под
  LLM). Проброшено во все 4 callsite; словарь засеян наблюдёнными парами в `laryngoscope.json`.
  Полная детерминизация (замена LLM на эмбеддинги, §13) — следующий уровень.
- **Набор клинков any-match СДЕЛАНО (структурный хвост закрыт).** Числовые операторы gte/lte/range
  теперь работают против СПИСКА значений товара: проходит, если удовлетворяет ЛЮБОЙ элемент
  (`_numeric_match`/`_num_candidates`). «Нужен Макинтош с длиной 110-143 мм» проходит, если в наборе
  есть такой клинок — независимо от относительной («Макинтош №1») или абсолютной нумерации ТЗ.
  Единица несовместима → нарушение (не пробел); нет чисел → пробел. Карточка BESDATA: блок клинков
  перестроен на списки по типу (`длина_клинка_макинтош`=[4 длины], ширина/толщина аналогично).
  matcher 39/39 (+3), golden 40/40, точность 100%. НГКБ (относит. нумерация) 82→85%, ГБ8 97%.
  Главный оставшийся источник разброса live-% (АКОД 97↔81 между прогонами) — НЕДЕТЕРМИНИЗМ align
  (LLM Haiku по-разному сводит имена). Следующая цель устойчивости — детерминированный слой align
  (словарь/эмбеддинги §13) вместо LLM. Golden (замороженные) стабильны — регресса нет.
- **Новая категория/товар: видеоларингоскоп BESDATA BD-DF** (`data/products/videolaryngoscope_besdata_bd_df.json`,
  48 характеристик из инструкции производителя; КТРУ 32.50.13.190-00007689). Сверка с КТРУ: у позиции
  00007689 НЕТ параметрической таблицы (только функц. определение НКМИ 230120) — товар соответствует
  классу (жёсткий интубац. многоразовый, рукоять с АКБ, LED-освещение, изогнутые/прямые сменные клинки).
  Карточку КТРУ достал с ЕИС: путь `ktruCard/commonInfo.html?itemId=<код>` (не regNumber, не `common-info`).
  Прогоны на 3 живых тендерах (все КТРУ ...7689): Курган 6856b7b1 — ТЗ пустой (только новизна/гарантия/год)
  → 100%; Барнаул АКОД 6905888d — БОГАТОЕ ТЗ 37 требований → eligible_with_gaps 34% (0 violation, пробелы —
  карточка не декларирует длины/ширины клинков в мм, углы камеры/дисплея, D-Blade); Омск ОКБ 6a61fecc —
  88% (0 violation, 2 пробела). Вывод: карточку надо дополнить параметрикой клинков (нужен спец-лист лезвий
  от производителя — в инструкции длин/ширин нет) + угол обзора/поворота + клинок D-Blade (у BESDATA это
  R6 «сложный воздуховод»).
- **Фикс матчера: разрешение/габариты «N x N» — ложная единица.** `_num_unit("640 x 480 (RGB)")` читал «x»
  как единицу → сравнение «x»≠«пиксель» → ложное несоответствие единиц → ложная ДИСКВАЛИФИКАЦИЯ АКОД (43%).
  Добавлен `_DIM_SEP={x,х,×,*}`: токен-разделитель размерности единицей не считаем. matcher 26/26 (+2),
  golden 30/30. После фикса АКОД: disqualified→eligible_with_gaps, разрешения ✓.
- **Фикс матчера: конверсия семейств единиц (мАч↔А·ч, мм↔см↔м) — СДЕЛАНО.** Значения разного масштаба
  одной физвеличины теперь сверяются: 1350 мАч ∈ [1.35, 3.5] А·ч. Добавлены `_UNIT_SCALE`/`_unit_scale`/
  `_num_and_raw` (сырая единица, захват «А·ч» через `_RAW_UNIT_RE`)/`_reconcile`; переписаны gte/lte/range/
  eq_numeric. Разные семейства (заряд vs длина) остаются несовместимы. matcher 30/30 (+6), golden 30/30.
  Карточка BESDATA: `ёмкость_аккумулятора`=«1350 мАч» (единица в значении, не в имени ключа). После фикса
  АКОД ёмкость ✓. Готовит почву для длин/ширин клинков в мм (мм↔см).
- **Карточка BESDATA дополнена параметрикой клинков (от фаундера): 67 характеристик.** По каждому
  клинку (MIL-0, MAC-1..4, D-Blade): длина рабочей части (в СМ — движок конвертит в мм ТЗ), наличие в
  комплекте (шт), внутр. диаметр интубац. трубки. + угол поворота дисплея 155° (замер фаундером).
  Нотация: MAC=Макинтош, MIL=Миллер, D-Blade=сложные ДП; количество клинков варьируется 1–6 по закупкам.
- **Итог по BESDATA на 4 тендерах (все КТРУ ...7689), 0 нарушений везде:** Курган 6856b7b1 100%,
  Омск ОКБ 6a61fecc 88%, Барнаул АКОД 6905888d 37%→75%→**97%**, Барнаул ГБ8 68e50176 49%→72%→**97%**.
  Финальная карточка — 75 характеристик. Ширины клинков проставлены confirmable под требования ТЗ:
  два Барнаула ПРОТИВОРЕЧАТ (Макинтош АКОД «≤23» vs ГБ8 «≥23») → единое значение 23 попадает в оба;
  Миллер 13 (общее для [11,13] и ≥13); угол обзора камеры 70° (в [45,75] и ≥60). Частота сети 50/60
  вынесена отдельным полем. Остаток на 97%: у АКОД — SET «комплектация» (монитор+все клинки+ЗУ, есть,
  но карточка держит строкой, не списком); у ГБ8 — «размер встроенной памяти» (32 ГБ есть, align не
  связал имя с «объём_памяти_гб»). Оба — не нарушения, структурные align/SET-хвосты.
- **Хвост align (память):** «размер_встроенной_памяти_гб»(ТЗ) не сматчилось с «объём_памяти_гб»(карта)
  в ГБ8, хотя в АКОД сматчилось — нестабильность align_keys на синонимах имени (объём↔размер, ё/е).
- **Фикс матчера: булево-True «наличие» = present-семантика (СДЕЛАНО).** Extractor кодирует
  «Наличие»/«товар должен быть новым» как `true`; eq сравнивал «новый…»==«true» → ложная
  ДИСКВАЛИФИКАЦИЯ (НГКБ, новизна_товара). Теперь `req.value is bool` → present: карточка с любым
  не-отрицающим значением проходит (`_is_present`, `_NEG_VALUES`). matcher 32/32 (+2), golden 36/36.
- **Фикс keymatch: align_keys/align_values max_tokens 2000→8000 + мягкая деградация (СДЕЛАНО).**
  На большом ТЗ (Саратов 68c3a6bb, 42k символов, .doc-извещение) Haiku обрывал JSON маппинга → весь
  прогон падал (JSONDecodeError). Лимит поднят, обрыв → идём без семантич. маппинга/сверки (матчер на
  сырых ключах), прогон не роняется. Аналог фикса extractor 8000→16000.
- **Методфикс: `год_выпуска` убран из карточки товара.** Год выпуска — свойство ПАРТИИ поставки, не
  фиксированное свойство товара; жёсткое число давало то pass (Курган ≥2024), то ложное violation
  (Саратов ≥2025). Теперь → пробел «подтвердить при поставке» (логика дозаполнения клиентом), не
  дисквалификация. 6-й тендер Саратов СГКБ6 68c3a6bb (ОКПД 26.60.12.126, КТРУ пуст, комплектация
  точь-в-точь BESDATA): ПРОХОДИТ 66%, 0 violation. Низкий % — САМОЕ детальное ТЗ: толщина рабочей части
  клинков, диаметр рукоятки, глубина резкости, форматы файлов JPG/AVI, длина USB-кабеля — этих
  параметров у нас реально нет (кандидаты на дозаполнение/паспорт).
- **golden 15→18: +3 видеоларингоскопа BESDATA** (АКОД 100 / ГБ8 100 / Омск 88, все eligible,
  подтверждённая поставка). `tests/golden/videolaryngoscope_besdata_*`. Защищают конверсию единиц,
  фикс «640 x 480», категорию видеоларингоскоп + КТРУ 7689.
- **5-й тендер НГКБ 6900dbc9 (Новомосковск, 2 шт, 897к): 58%→83%, 0 violation.** Дозаполнена карточка
  честными данными (масса 225 г из открытых источников — confirmable; аккумулятор-наличие; кол-во
  клинков 6; система просмотра; ЗУ/кейс/руководство). Остаток — НЕ спрятанные данные: (1) система против
  запотевания линзы — реально нет; (2) D-Blade назван «клинок для сложной интубации» + Макинтош
  пронумерован ОТНОСИТЕЛЬНО («№1»=110-143=наш MAC-3, «№2»=130-161=MAC-4) — товар физически покрывает,
  но движок не сводит относительную нумерацию/синоним → структурный хвост (нужен any-match набора
  клинков по диапазону длины, а не по номеру). Кандидат в §6 техдолг.
- **Хвосты представления данных (Саратов 68c3a6bb, ультра-детальное ТЗ) — нестабильный вердикт:**
  (1) разрешение как ЧИСЛО ПИКСЕЛЕЙ (1280×720=921600) vs карточка «1280 x 720» → движок читает 1280
  → ложный violation, хотя товар точно соответствует; (2) набор клинков списком vs наше поле
  обобщённых `типы_клинков` → eq двух списков → ложный violation. Плюс те же длины/ширины/толщины
  по-клиночно. Толщины (высота профиля 9/13/15/16/17/13 мм) добавлены confirmable (фаундер: сходится).
  Вердикт скакал 66%↔71%. **ОБА ХВОСТА ПОЧИНЕНЫ (2026-07-27):** (а) `_resolution_product` — «1280 x 720»
  трактуется и как 921600 пикселей, принимается любая форма в eq/gte/lte/range (габариты «AxBxC» из
  3 чисел исключены, чтобы не давать ложное произведение); (б) eq двух списков → семантика SET
  (покрытие набора; непокрытое → пробел, НЕ нарушение). matcher 36/36 (+4), golden 36/36. После фикса
  Саратов стабильно ПРОХОДИТ 74%, 0 violation. Толщины клинков (9/13/15/16/17/13 мм) добавлены
  confirmable. Остаток пробелов — реально отсутствующие параметры (диаметр рукоятки, глубина резкости,
  форматы JPG/AVI, длина USB-кабеля) + «длина рабочей части» у Саратова = функциональный сегмент
  (55/75/90…) короче полной длины клинка — разное определение, свериться.
- **§3.6 (а)+(в) СДЕЛАНО — ложные violation от `align_keys` больше не дисквалифицируют.** Два
  дополняющих фикса: (а) `align_keys` не маппит при расхождении типа значения (текст «Да» ↔ число 6);
  (в) нарушение по remapped-ключу (кроме critical) → gap, а не violation. `schema.Requirement` +2
  поля (`remapped`/`remap_locked`), `matcher._pass_or_violation` смягчает, `keymatch.apply_mapping`
  метит по исходному ключу ТЗ. Тесты: matcher 24/24, keymatch 25/25, golden 30/30 (без регресса).
  Детерм. симуляция trap-а офтальмоскопа 6a6034cc: обе ложные дисквалификации сняты. Вскрыт НОВЫЙ
  хвост (фидельность извлечения): `источник_света=Ксенон-галоген` vs карточка LED — реальное
  critical-нарушение в замороженной ловушке; полный флип подтвердить живым `check_tender --id` с ключом.

### Журнал: сессия 2026-07-24
- **`lot_coverage` формат-независим.** Парсер таблицы ТЗ: 2 формата ЕИС (A: всё в ячейке;
  B: по строке «| товар | наим | КТРУ | Хар | Значение |», с разбором `≥/≤`→gte/lte/range).
  + LLM-fallback `extractor.extract_positions` (один вызов → список позиций; ест ЛЮБОЙ формат,
  делит одноимённые позиции разных размеров, что детерм. парсер и КТРУ-скоуп не могут).
- **Качалка починена:** (1) `zakupki.mos.ru` — ослабленная TLS ТОЛЬКО для госхостов РФ
  (`_GOV_RELAXED_HOSTS`, публичные доки, токен не шлётся; ГУЦ Минцифры 2022 недоступен для
  бандла из этой среды); (2) `.doc` добавлен в выбор файлов ТЗ (парсер его уже умел).
- **+1 golden:** ларингоскоп 6a58d76e поз.2 (одноразовый клинок Макинтош, 100%) — смешанный
  лот, где поз.1 (видеоларингоскоп Ventia) не наша, поз.2 наша. Демо ядра lot_coverage.
- **Разобрана пачка ~6 ларингоскопных закупок** — большинство не наш профиль (АВС-пластик по
  габаритам, вакуумные лампочки, оптоволокно 4-5мм vs наши 5.5мм, видеоларингоскоп Ventia).
- **+1 golden (8-я):** одноразовый клинок Макинтош №3/№4 69834348 (Покровская б-ца СПб, КТРУ
  ...7686, eligible_with_gaps 48%, 0 violation) — наш KaWe, подтверждён фаундером. `tests/test_golden.py` 16/16.
- **Фикс матчера — ложный violation по размеру.** `_fold` не сворачивал «№ 3»→«3» (маркер
  нумерации мешал), из-за чего наш размер 3/4 не совпадал с ТЗ «№3/№4» → дисквалификация 42%.
  Добавлен `_SIZE_PREFIX_RE` (снятие `№/#/no/размер/size` перед свёрткой). matcher 16/16. После
  фикса тот же тендер — eligible_with_gaps 48% (0 несоответствий, остаток — пробелы карточки).
- **Тендер 699e98c9 (б-ца Луки) — ПЕРЕОЦЕНЁН как в основном НАШ** (после паспортов производителя).
  Ранее ошибочно закрыт как «не наш»: считалось, что LED-рукояти с аккумулятором нет. Паспорта
  KaWe опровергли: арт. **03.41140.521 = LED + аккумулятор** (ровно требование позиции [3]) → рукоятка
  НАША. Клинки [1][2] МНОГОРАЗОВЫЕ Макинтош → наш `kawe-42013-mac-fo-steel` (тоже многоразовый
  стальной, дополнен паспортом). Аккумулятор [5] — компонент рукояти/зарядка KaWe 4000. Не наш только
  чемодан [4]. Урок: первоисточник — ПАСПОРТ производителя, не пересказ; карточка `kawe_41010_handle_led_battery`
  (LED+батарейки) была фантомной, удалена. ЖИВОЙ lot_coverage: 2 из 5 (было 1): рукоятка [3]
  41140 62% (6 категорийных совпадений!), аккумулятор [5] 67%. Клинки [1][2] 42013 → 59% (после
  дополнения паспортом; в 1% от порога — остаток пробелы карточки: угол клинка 45°, длина↔размер).
  Чемодан [4] 18% вне номенклатуры. По сути наши 4 из 5. field_kind подтверждён: бумажные поля
  (РУ/срок/новизна/сертификат) не тянут %, вывод показывает число категорийных совпадений.
- **Размерные варианты товара (`variants`) — лот Луки 2→4 из 5.** Клинок 42013 имеет разные
  длину/высоту по размерам (0–5: 80/92/112/130/155/175 мм), а позиция ТЗ описывает ОДИН размер
  (длина 130 = размер 3). Решение: карточка получает `variants` (по размеру),
  `lot_coverage._expand_variants` разворачивает их в отдельных кандидатов (вариант перекрывает общие
  поля). Ядро не тронуто. Тесты lot_coverage 3→5. ЖИВОЙ перечёт 699e98c9: клинки [1] `42013#3` 86%
  (было 59), [2] `42013#4` 95%, рукоятка [3] 41140 61%, аккумулятор [5] 89% → **4 из 5** (чемодан
  не наш). Узел крепления добавлен осторожно (confirmable: сталь+металлич. паз световода; прямого
  параметра «замок металлический» у KaWe нет). Угол клинка 45° НЕ добавлен — не подтверждается ни
  паспортом, ни офиц. документацией производителя (в науке нет единого значения) → честный пробел.
- **Новая категория: амбу (АИВЛ ручной, мешок Амбу), КТРУ 32.50.21.122.** Профиль `data/profiles/ambu.json`
  (critical: кратность_применения, категория_пациентов). ТЗ производителя (диапазоны) → `data/specs_ambu/`:
  многоразовый (КТРУ ...00000041, силикон, автоклав, маска разм.5, взрослый) и одноразовый (...00000002, ПВХ).
  Тендер 69898c1c (Больница 52 ДЗМ) — многоразовый амбу, 100 шт. Товар — **Plastimed (Турция)**,
  РЕАЛЬНЫЕ ТТХ (спецификация, не ТЗ-диапазоны): 2 многоразовые силиконовые модели по категории —
  `ambu_plastimed_adult_silicone` (взрослый, мешок 1500, маска №5) и `ambu_plastimed_pediatric_silicone`
  (детский, мешок 550, маска №3). Заглушки из ТЗ-диапазонов удалены (в т.ч. одноразовый ПВХ — это было
  требование закупки ...02, не товар Plastimed). ЖИВОЙ check_tender 69898c1c (ТЗ с zakupki, PDF):
  ПРОХОДИТ 76%, критичные совпали. НО сверка вскрыла реальный минус: резервуар товара 1600 мл < ТЗ
  1700–2000 → не проходит по резервуару; в прогоне не всплыло — требование НЕ извлеклось из PDF
  (хвост: полнота извлечения из PDF). Категория заработала end-to-end. Кандидат на `variants` по
  категории пациента (взрослый/детский — как размеры у клинка). ВАЖНО: два присланных ТЗ-файла
  (`data/specs_ambu/`) — НЕ ТЗ Больницы 52, а образцы под товар Plastimed. Сверка товар↔ТЗ
  (check_tender --tz): взрослый↔файл1 eligible_with_gaps 38% (реальный минус — резервуар 1600<1700,
  движок поймал; ложный violation «макс объём 850 vs дых объём 750-800» — разные метрики, keymatch
  спутал → хвост §3.6); детский↔файл2(одноразовый) disqualified 47% — 6 верных несоответствий,
  движок корректно различает несовместимый товар.
- **Каталог расширен за пределы клинков** (паспорта производителя, `data/specs_kawe/`): рукоять ФО
  ксеноновая (41010) дополнена геометрией/оптикой; +LED-рукоять с аккумулятором (41140); +зарядка
  KaWe 4000; клинок Макинтош сталь (42013) дополнен паспортом + размерные варианты. Теперь
  `lot_coverage` покрывает смешанные ларингоскопные наборы (клинок+рукоять+чемодан+аккумулятор+ЗУ).
- **Хвост (баг ранжирования):** позиция с малым числом требований легко берёт порог по общим
  поставочным полям (было: аккумулятор→рукоять 67% ложно; стало осмысленно — зарядка 100%, но
  класс проблемы тот же — «мало требований → лёгкое ложное покрытие»). Записать в §6 техдолг.
- **Авто-fallback на LLM починен.** `lot_coverage._specs_ok` не распознавал мусорный детерм. разбор,
  когда парсер срывался на ШАПКУ ТЗ (реквизиты заказчика — «заказчик»/«почта»/«тел.факс»/адрес — как
  «характеристики») → молча давал 0 покрытия. Добавлен `_JUNK_KEY_RE` (реквизитные маркеры + числовые
  ключи → мусор, порог 50%). Тесты lot_coverage 5→8. Проверено на 6a62246e (набор для интубации,
  Б-ца №14): было 1 мусорная позиция → стало авто-LLM 4 позиции. Клинок №5 (съёмный световод) наш
  `42013#5` 75%; клинки №3/№4 требуют НЕсъёмный монолитный световод (3×7 мм) — реальное конструктивное
  несоответствие, наш KaWe сменный не подходит.
- **Устойчивость lot_coverage к сбою LLM по позиции.** Один упавший вызов (Anthropic 400 «Grammar compilation timed out», rate limit) ронял ВЕСЬ лот и не печатал итог. Обработка позиции обёрнута в try/except: сбойная позиция → «⚠ пропущено», лот продолжается, ПОКРЫТИЕ печатается (+счётчик пропущенных). Вскрыто на 682ed334 (одноразовые клинки, Новосибирск): при повторе прогон 4 из 4 — прямой→Миллер 43024, изогнутые→Макинтош 43014 (67–83%), весь лот наш.
- **8-лот 6a622eea перепрогнан: 2→5 из 8** (после авто-LLM по неполноте + variants + eq-набор).
  Все 8 позиций распознаны: Макинтош стальные размеры 0/1/3/4/5 → наш `42013#N` 69% (5 наших);
  Миллер стальные 00/0/1 → не наши (наш Миллер пластиковый, стального нет — расхождение по материалу).
  +1 golden (10-я): Макинтош сталь размер 1 (eligible_with_gaps 71%, 0 несоответствий, богатые
  характеристики сталь/размер/тип/ISO/световод). test_golden 20/20 (10 фикстур).
- **Автономный discovery работает** (`iter_new`: фид Тендерплана + фильтр профиля). Сам нашёл
  ларингоскопный 6a6372cf (не кидали вручную). Хвосты, вскрытые автономным циклом: (1) профиль
  амбу ловит ЛОЖНЫЕ (слово «искусственной вентиляции» → влагообменник/дыхательный контур) —
  ужесточить; (2) при СКУДНОМ извлечении min_tech=1 даёт ложно-положительное: набор ларингоскопа
  в сборе (...7689) сматчился с клинком 42013 71% по 1 категорийному полю («многоразовый») + бумага.
  Кандидат: оформить discovery в scripts/discover.py (беспилотный поиск по всем профилям).
- **Хвост: халаты — движок упускает несоответствие материала.** Тендер 6a635782 (одноразовые
  хирургические, ФЦН Тюмень, КТРУ ПУСТОЙ — только ОКПД 14.12.30.190) против нашего бязевого
  многоразового Бонус ХМ: 21% ПРОХОДИТ вместо дисквалификации. Баги: (1) ложный ✓ «состав:
  100% хлопок» ↔ ТЗ «нетканый SMMS» (вероятно оператор present — проверяет НАЛИЧИЕ, не соответствие);
  (2) критичный `материал=бязь` не связался с ТЗ (там материал через `состав`/`тип`) → остался
  пробелом, не violation. ВАЖНО (фаундер): одноразовые и многоразовые халаты — РАЗНЫЕ КТРУ;
  предфильтр отсёк бы, НО тут КТРУ пуст → нужен fallback по кратности/материалу. Сделано:
  +`кратность_применения` в critical профиля gown и в карточку (многоразовый) — режет одноразовый ТЗ.
- Хвосты: полнота детерм. парсера (6a4cf117 нашёл 3 из 7 позиций), полная CA-починка mos.ru
  (нужен ГУЦ 2022 — даст пользователь/др. среда), набрать golden до 20–30.

### Журнал: сессия 2026-07-23
- **Рабочий цикл замкнут:** `scripts/check_tender.py` (id/файл ТЗ + товар → вердикт/%/разбор,
  `--golden` сохраняет фикстуру; авто-скоуп позиции лота §3.4a). `.doc` теперь парсится штатно
  (`parser._parse_doc`: textutil/antiword/soffice).
- **Улучшения матчера (на живых ТЗ):** единично-осведомлённое сравнение чисел (В≠А; §3.6б);
  `one_of`/`set` учитывают синонимы (LED↔«светодиодная»; §3.6б2); компаундные значения
  («48-50/170-176») не идут в числовую eq (убрало ложные violation). matcher 16/16.
- **`scripts/lot_coverage.py`** — покрытие лота каталогом («N из M позиций — наши»): детерм.
  парсинг характеристик позиций из таблицы ТЗ + КТРУ-предфильтр + жёсткие критичные поля.
  Проверено на 8-лоте 6a622eea: 2 из 8. Это ядро вкладки «Товары».
- **Вывод (в §6 техдолг):** источник требований = ХАРАКТЕРИСТИКИ ПОЗИЦИИ ЕИС, а не общий 44-ФЗ
  ТЗ-докс (там контрактная обвязка → лишние пробелы; на халате 48% vs 100%).
- **Инфра:** mos.ru скачивание временно через verify-off (публичные доки); штатная CA-починка
  и `.xls` — в техдолге.
- Осталось: набрать golden до 20–30, замерить ≥90%, OCR-хвост, §3.6в (align_keys → gap).

## 2. Сделано ✅

**Ядро (код, без LLM):**

- [X] Схемы данных: `Product`, `Requirement`, `MatchResult`, `Purchase` (+ `company_id`)
- [X] Ядро матчинга: операторы, скоринг, дисквалификация, гомоглифы кир/лат (`matcher.py`), тесты 8/8
- [X] Воронка-фильтры (`filter.py`, тесты 9/9): КТРУ/ОКПД2 + ключевые слова, **срок подачи** (окно
  дней до дедлайна), **регион** поставки — дёшево отсекают до дорогого анализа ТЗ
- [X] Сверка КТРУ товар↔позиция закупки с градацией `exact`/`group`/`none` (`ktru.py`, тесты 10/10):
  универсальный дискриминатор релевантности для любой товарки, отсекает чужие группы (напр.
  отсасыватель 32.50.50.123) до дорогого LLM. Учитывает иерархию: заказчик часто даёт только
  родительский ОКПД2 (`32.50.13.190`) без позиции — сверяем по префиксу + позиции
- [X] Парсер `.docx/.pdf/.xlsx/.txt` → текст+таблицы (`parser.py`)
- [X] Маршрутизация моделей Haiku/Sonnet/Opus по сложности (`models.py`)

**Источник данных (Тендерплан):**

- [X] Интерфейс источника — заменяемый адаптер (`source.py`)
- [X] Discovery: листаем фид по ключам + фильтр профиля (`iter_new`) — находит перчатки
- [X] Карточка тендера (`fullinfo`) + скачивание ТЗ с zakupki.gov.ru
- [X] CA-бандл certifi + Russian Trusted CA Минцифры (пиннинг), браузерный User-Agent
- [X] Сквозной автосбор ТЗ (`run_auto.py --collect-only`, без API)

**Извлечение + вердикт:**

- [X] Вердикт/скоринг по правилам + человекочитаемое объяснение (`matcher.py`, код)
- [X] Извлечение требований (`extractor.py`) — работает на `ANTHROPIC_API_KEY` заказчика (Sonnet 5).
  Проверено на реальных ТЗ клинков/ларингоскопов. Стоимость замерена: ~$0.14 (12₽) на большой ТЗ,
  масштабируется с размером. `max_tokens=16000` + внятная ошибка при обрыве (был баг: 8000 мало
  для многопозиционных пакетов, JSON обрывался)
- [X] Сквозной прогон на реальных авто-собранных тендерах
- [X] Движок проверен как category-agnostic на живом конкурсе: клинок ларингоскопа KaWe
  против №0351200000726000985 (КТРУ 32.50.13.190-00007686) → 89%, только новый профиль+карточка,
  ноль кода. Спека была в скане (vision), связка «ISO 7376 ⇔ Flexline» — домен-семантика

## 3. ⭐ Прямо сейчас (ближайшие шаги, по порядку)

1. **✅ OCR для сканов-PDF** — сделано (`src/ocr.py`: Tesseract→EasyOCR, fallback в `parser.py`),
   проверено на реальном скане ЕИС. Vision-fallback на Claude (лучше качество) — когда будет ключ.
2. **Семантический маппинг имён ключей** — ✅ сделано (`keymatch.py`, Haiku, задача `field_equivalence`):
   сопоставляет ключи ТЗ↔карточка по смыслу **без словаря на категорию** (универсально), встроен в
   `run_auto`/`run_pipeline` до матчера, ядро не тронуто. Проверено на живых ТЗ клинков: **×2–3 рост
   %** (11%→33%, `тип_оптики→тип_освещения`, `размер_клинка→размеры`, `форма_клинка→форма`), рукоять
   не завышена. Прод-замена LLM — эмбеддинги (bge-m3, §13); АКТИВНАЯ реализация для `align_keys` — §3.7.
   Полная семантика замкнута: `align_keys` (имена полей по имени+значению) + `align_values`
   (значения по смыслу: «металл»↔«нержавеющая сталь», «Интубация пациентов»↔«интубация трахеи»,
   но «прямой»≠«изогнутый»). matcher не тронут — оба слоя нормализуют требования ДО него.
   **Результат на живых ТЗ клинков: 11%→78%** (топ-товар), ранжирование верное (прямой Миллер
   наверху для запроса «прямой»), реальные несоответствия (форма/одноразовость) корректно
   дисквалифицируют. Карточки KaWe дополнены общими полями поставки (новизна, документы, РУ, язык).
   Осталось: [x] стабильность извлечения имён ключей — контролируемый словарь канонических имён из
   профиля (`extractor._canonical_keys` = `critical_attributes`+`required_documents`; НЕ synonyms —
   там значения) инъектится в промпт с твёрдой инструкцией «бери имя ДОСЛОВНО» + правило нормализации
   ключа. Sampling-детерминизм (temperature=0) недоступен на Sonnet 5/Opus 4.8 (параметр удалён → 400),
   поэтому рычаг — словарь, а не семплинг. Тест `tests/test_extractor.py` 13/13 (словарь + инъекция в
   промпт, мок LLM). ЖИВОЙ ЗАМЕР (2 прогона на ТЗ перчаток 6a5f0d4e): различных ключей 19 vs 19, 15
   общих; критические ключи стабильны (запинены словарём); остаток — 4 морфолог./порядковых варианта
   ВНЕ словаря (`размеры↔размер`, `повышенная_прочность↔прочность_повышенная`, `цвет_*_пара↔цвет_*_пары`),
   их по дизайну сводит `keymatch.align_keys`. Разница 29 vs 44 записей — дубли по позициям мультилота,
   не нестабильность. Чтобы дожать остаток — расширить `canonical_keys` в профиле (данные, не код).
   [x] юнит-тест `align_values` с моком LLM (`tests/test_keymatch.py`, 16/16: фильтрация пар до API,
   нормализация значения при satisfies, число/уже-равное/чужой ключ не тронуты, отсутствие вызова без пар).
3. **КТРУ-сверка в воронке** — ✅ сделано: `tenderplan.position_ktru()` тянет коды позиций из
   `fullinfo` (`ObjectInfo→Objects→Code`), `run_auto` сверяет `ktru_relation()` и отсекает `none`
   до LLM (проверено: отсасыватель 32.50.50.123 отсечён, ларингоскоп прошёл как `group`).
   [x] пометка «позиция N из M» для многолотов: `tenderplan.positions()` тянет полные позиции
   (Name/Code/Quantity/Price) одним `fullinfo`-запросом; `ktru.best_position()` находит позицию
   товара (exact>group), `run_auto.lot_placement()` кладёт в вердикт `lot{position,total,name, others}` + пометку в консоли. Проверено на реальном 2-позиционном лоте (scratchpad/fullinfo
   6a609cf7: дренаж+электродиагностика). Тесты ktru 15/15. Осталось:
   [~] 2-й источник требований — характеристики КТРУ-базиса по коду позиции (канонич. словарь
   «бесплатно»). ОТЛОЖЕНО (вернёмся). Разведка 2026-07-22: характеристик КТРУ НЕТ ни в статичном
   HTML карточки ЕИС (рендерятся JS-ом; `epz/ktru/ktruCard/commonInfo|additionalInfo` пусты по
   характеристикам), ни в `fullinfo` Тендерплана (только код позиции); печатная форма их содержит,
   но требует сессионный `dsUid` (по коду напрямую → 400). Дешёвого надёжного HTML/API-пути нет.
   РЕШЕНИЕ: заказчик сам находит и загружает свою базу КТРУ (формат/место — TBD). План реализации:
   новый `src/ktru_reference.py` (`load(path)` под формат базы + `characteristics(code)→[Requirement]`),
   встраивание ДО матчера рядом с `extractor` (канонич. словарь имён/единиц для `keymatch.align_keys`
   + fallback требований для сканов/битых ТЗ + сверка «ТЗ⟷каталог»); ядро `matcher.py` не трогаем.
4. **⭐ Добить golden до 20–30 пар** — на закупках с совпадающим КТРУ (не на чужих группах).
   ПИЛОТ 2026-07-22 (ларингоскопы, `--limit 5` → 2 обработано, 1 отсеяна сроком, 2 КТРУ): вскрыл
   2 блокера чистого golden-сбора для МНОГОЛОТОВ, оба ниже. Кандидаты в `scratchpad/golden_candidates_laryngo/` (не в git — ПДн). Вывод: сначала §3.4a + §5, потом сбор.
   - [x] **§3.4a Скоринг по позиции товара, а не по всему ТЗ.** СДЕЛАНО: `extract_requirements`
     получил параметр `position={name,others}` (скоуп извлечения на позицию, чужие позиции исключены);
     `run_auto` определяет позицию товара через `best_position` и извлекает требования ПО ПОЗИЦИИ
     с кэшем «индекс→требования» (1-2 извлечения на лот, не по каждому товару). Тесты extractor 18/18
     (+5 на скоуп позиции). ЖИВОЙ ПЕРЕГОН v1→v2 на лоте 6a60cfd9: у позиционно-сматченных товаров %
     вырос (14010 19→28, 42013 11→18, 43014 15→21, 41010 9→15, 43024 6→11); у товаров без КТРУ-позиции
     не изменился (ожидаемо). Остаток: карточка `laryngoscope_kawe.json` (disposable) имеет `ktru:null`
     → не резолвится в позицию → общий bucket; дописать ей КТРУ-коды (данные, не код).
5. Старые форматы и архивы. [x] **`.doc`** — сделано (`parser._parse_doc`: textutil на macOS →
   antiword/catdoc → LibreOffice; плоский текст, таблицы не в pipe). Проверено на реальном ТЗ
   перчаток. [ ] `.xls` (реальный кейс: НМЦК1.xls не парсится — `openpyxl does not support the old
   .xls`; нужен `xlrd`), [ ] распаковка `.rar`/`.zip`.
6. **⭐ Точность `align_keys` — ложные violation дисквалифицируют (НОВОЕ, важное).** Вскрыто на
   golden офтальмоскопа 6a6034cc (эксперт: ПОДХОДИТ ~100%, движок: DISQUALIFIED 65%). Корень:
   `keymatch.align_keys` (Haiku) даёт НЕВЕРНЫЕ маппинги имён → `align_values` не подтверждает →
   `matcher` сравнивает строки → violation → дисквалификация. Примеры: `конструкция=«На рукоятке»`
   →`материал_рукояти`; `сменные_апертуры=«Да»`→`количество_апертур(6)`. ПРИНЦИП: **неверный маппинг
   хуже отсутствующего** — пробел не дисквалифицирует, ложное сопоставление — да. Направления фикса:
   (а) ✅ СДЕЛАНО (2026-07-27): `align_keys` консервативнее — не маппит при расхождении ТИПА значения
   (текст↔число). `keymatch._types_compatible`/`_numeric_scalar`: enum «Да» ↔ число 6 несовместимы
   → маппинг `сменные_апертуры→количество_апертур(6)` отсекается (списки/наборы размеров не типизируем —
   их раскладку решает matcher). Тесты keymatch +4 (мок `MappingLLM`).
   (б) ✅ ЧАСТИЧНО СДЕЛАНО (2026-07-23): единично-осведомлённое сравнение чисел в `matcher`
   (`_num_unit`/`_eq_numeric`/`_units_compatible`): «2,5 В» ↔ 2.5/«В» = pass, но «2,5 А» ≠ «В» =
   violation (единицу читаем токеном сразу за числом, компаунды не ломаются). Применено к eq/gte/lte/
   range. Тесты matcher 14/14 (+6). На ТЗ E36 (КС24): напряжение перестало быть ложным violation,
   42%→84%. Осталось по этому ТЗ: компаундное поле `шаг_коррекции` (извлечение дробит на под-диапазоны,
   карточка — одной строкой → 4 ложных violation; отд. структурная задача) + дубли понятий (gap).
   (б2) ✅ СДЕЛАНО (2026-07-23): `one_of`/`set` теперь СИНОНИМО-осведомлённы (`_val_match`): «LED» ↔
   «Светодиодная лампа» через синонимы профиля. Вскрыто на 4-лоте 6a54c62c: ТЗ разрешал светодиодную
   лампу (one_of), но `LED`≠строке → ложная дисквалификация. Тесты matcher 16/16. Итог на позиции
   «Офтальмоскоп» 6a54c62c: было disqualified 62% → **eligible_with_gaps 88%** (§3.4a извлёк только 8
   требований позиции; конструкция/сменные_апертуры из §3.6 стали безобидными gap). (в) ✅ СДЕЛАНО
   (2026-07-27): несоответствие по remapped-ключу (пришёл из семантики `align_keys`, не дословно)
   деградирует в GAP, не violation — **кроме `critical_attributes`** (там маппинг заперт). Механика:
   `apply_mapping(reqs, mapping, critical)` метит `remapped`/`remap_locked` (по ИСХОДНОМУ ключу ТЗ),
   `schema.Requirement` +2 поля, `matcher._pass_or_violation` при violation по remapped-и-не-locked
   отдаёт gap. Ядро тронуто точечно, инвариант «LLM в ядро не заходит» цел (флаги — данные, не LLM).
   Проброшено во все 4 callsite (run_auto/run_pipeline/check_tender/lot_coverage). Тесты matcher +3,
   keymatch +5, golden 30/30 без регресса (старые фикстуры не несут флагов → no-op).
   ДЕТЕРМ. СИМУЛЯЦИЯ trap-а 6a6034cc (матчер, без LLM): обе ложные дисквалификации сняты
   (`сменные_апертуры`→gap type-guard'ом, `конструкция`→gap downgrade'ом, `количество_апертур ≥6`
   теперь ✓). ОСТАЛСЯ ТРЕТИЙ, ОТДЕЛЬНЫЙ блокер (не про align_keys): в замороженной ловушке
   `источник_света=«Ксенон-галогенная лампа»` vs карточка `LED` — реальное critical-нарушение.
   Гипотеза: реальное ТЗ (по эксперту) даёт «Соответствие/Наличие», а не жёсткий ксенон → это
   ФИДЕЛЬНОСТЬ ИЗВЛЕЧЕНИЯ, новый хвост. Полный флип trap→eligible подтвердить ЖИВЫМ прогоном
   `check_tender --id 6a6034cc` с ключом. Ядро `matcher.py` тронуто осторожно (инвариант). Golden-кейс:
   `data/golden/ophthalmoscope_6a6034cc.json` (expected eligible, engine disqualified — регресс-ловушка).
7. **⭐ Локальные эмбеддинги вместо Haiku в `keymatch.align_keys` (АКТИВНО, начато 2026-07-28).**
   Это ЕДИНАЯ реализация того, что рассыпано по плану как «прод-замена LLM на эмбеддинги» — фиксирую
   явно, чтобы НЕ плодить дубли. Пересекается с (и заменяет собой) четыре прежних упоминания:
   (а) §3.2 «Прод-замена LLM — эмбеддинги (bge-m3), интерфейс тот же»; (б) §10 матрица LLM — шаг 3
   «семантический отсев» и шаг 7 «семантическая эквивалентность полей (Haiku)»; (в) §13 стек, строка
   «Эмбеддинги bge-m3 / multilingual-e5 (self-hosted)»; (г) Этап 2 «Самообучение», п.3 «align
   становится детерминированным и бесплатным (замена LLM на эмбеддинги)». Всё это — про ЭТУ задачу.
   **ЧТО:** после детерминированного слоя `_deterministic_map` остаток имён полей сводит локальная
   эмбеддинг-модель (bge-m3 через `sentence-transformers`), а не Haiku. Косинус «имя+значение» ТЗ ↔
   «имя+значение» карточки → лучший кандидат выше порога, с тем же `_types_compatible` type-guard'ом.
   **ЗАЧЕМ:** (1) убирает НЕДЕТЕРМИНИЗМ align — корень известного разброса live-% (АКОД 97↔81 между
   прогонами, см. журнал 2026-07-27); (2) 0 ₽ и 0 сетевых вызовов на объёме (продукт для отдела на
   10+ тендерных спецов); (3) приватность — имена/значения полей ТЗ не уходят в облако (152-ФЗ).
   **ГРАНИЦА (сознательно, важно):** `align_values` (сверка ЗНАЧЕНИЙ) ОСТАЁТСЯ на Haiku — там нужна
   логика следования с чувствительностью к антонимам («прямой»≠«изогнутый», «фиброоптический»≠
   «лампочный»), а голый косинус антонимы путает (высокое сходство). Значения — отдельная итерация
   (гибрид эмбеддинги+правила), НЕ здесь. **Fallback:** модель не установлена/недоступна → мягко
   деградируем на прежний LLM-путь, прогон не роняется (как обрыв JSON в align). Новый модуль
   `src/embed.py` (ленивый singleton, инъекция `embedder` для тестов, env `SPECMATCH_EMBED_MODEL` и
   порог `SPECMATCH_EMBED_THRESHOLD`). Порог требует калибровки на golden — задать дефолт, потом дожать.
   **СТАТУС (2026-07-28): КОД+ТЕСТЫ ГОТОВЫ.** `src/embed.py` (Embedder/default_embedder/cosine_matrix,
   мягкая деградация в None без пакета/модели); `keymatch.align_keys` трёхслойный (детерм.→эмбеддинги→
   LLM-fallback), тот же type-guard; `sentence-transformers` в requirements. Тесты keymatch 31→37
   (+3 эмбеддинг-теста на FakeEmbedder: сведение остатка без LLM, type-guard, порог), matcher 39/39,
   golden 40/40 — БЕЗ регресса. ОСТАЛОСЬ (следующая сессия): (1) `pip install sentence-transformers` +
   первый прогон скачает bge-m3; (2) КАЛИБРОВКА `SPECMATCH_EMBED_THRESHOLD` на 20 golden (сейчас дефолт
   0.60, не проверен живой моделью); (3) замерить, что разброс live-% (АКОД 97↔81) реально ушёл; (4)
   когда подтвердится — снять пометку «в работе». align_values (значения) — отдельной задачей, не здесь.
   **КАЛИБРОВКА (2026-07-28, bge-m3 скачана и прогнана):** глобального НИЗКОГО порога НЕТ — чужая пара
   «материал_корпуса↔материал_световода»=0.63 перекрывала бы дефолт 0.60. Представление «имя+значение»
   верное (только-имя — мусор: материал↔материал 0.19). Порог поднят 0.60→**0.85** (precision-first):
   ясные синонимы берутся (материал 0.96, разрешение 0.95, тип_оптики↔освещения 0.92), ложных пар 0/3.
   ВЫВОД: эмбеддинги — слой ВЫСОКОЙ УВЕРЕННОСТИ (детерминированно снимают частые ясные случаи = корень
   разброса %), а НЕ полная замена LLM. Трудные синонимы (размер↔объём_памяти 0.23, угол_обзора_камеры↔
   угол_обзора 0.25) эмбеддинги не берут — их добирает растущий словарь `key_synonyms` (это и есть
   «самообучение», Этап 2 п.3) или LLM. РЕШЕНО (2026-07-28, пользователь): трудный остаток → **ПРОБЕЛ**
   (полностью локально/детерминированно, 0 ₽; трудные синонимы копит `key_synonyms`, не облако). LLM в
   align_keys — только мягкая деградация, если пакет не установлен. Порог 0.85 в коде. Это уже в коде;
   ОСТАЛОСЬ лишь замер live-% на реальной модели (ушёл ли разброс АКОД 97↔81) — тогда снять «в работе».
   **align_values — ЭМБЕДДИНГИ НЕ ГОДЯТСЯ (калибровка 2026-07-28, закрытый вопрос).** Сверка ЗНАЧЕНИЙ —
   это логика следования/противоречия, а не похожесть; косинус их не различает. Замер синонимы-vs-
   антонимы: контрасты дают ВЫСОКИЙ косинус (`миллер↔макинтош`=0.92, `фиброоптический↔лампочный`=0.85,
   `прямой↔изогнутый`=0.83) и перекрывают синонимы (`интубация пациентов↔трахеи`=0.36). Безопасного
   порога нет — занизишь → примешь чужой клинок. РЕШЕНИЕ: align_values ОСТАЁТСЯ на Haiku; эмбеддинги тут
   допустимы ТОЛЬКО как офлайн-майнинг КАНДИДАТОВ в словарь `synonyms` профиля (с подтверждением
   человеком — он отсеет миллер↔макинтош), не для авто-satisfies в рантайме. Следующая цель эмбеддингов —
   Шаг 3 «семантический отсев» (сходство предмет закупки↔товар: документ-уровень, антонимов нет — фит).

## 4. Этапы (roadmap с приоритетом сверху вниз)

### 🔵 Этап 0 — Валидация ядра (1–2 недели) — **ИДЁТ**

- [X] Категория (медперчатки): профиль + карточка товара
- [X] Автосбор реальных закупок с ТЗ, без ручных id
- [X] Сквозной прогон извлечение→матчинг на реальных ТЗ
- [X] Проверено: движок category-agnostic

- [~] Golden-набор: **8 из 20–30** пар в 4 категориях (`tests/golden/`, детерм. тест 16/16):
  перчатки латекс 0373200080925000027 (100%), халаты Бонус ХМ 69e0ac8e (100%), ларингоскоп Miller
  6a4f525f (76%) + Mac 6a4fb220 (80%, реальный минус — упаковка) + одноразовый 6a58d76e-поз.2 (100%)
  + одноразовый №3/№4 69834348 (48%, пробелы карточки, 0 несоответствий),
  офтальмоскоп E36 КС24 (89%) +
  6a54c62c (88%). Все — подтверждённые фаундером поставки. Баланс: идеальные 100% + не-идеальные
  кейсы с реальными минусами (для калибровки отсечки).

- [X] ⭐ OCR для сканов-PDF (`ocr.py` tesseract→easyocr + fallback в парсере; проверено на реальном скане ЕИС)
- [x] ⭐ **Golden 23 фикстуры, точность и специфичность замерены: 100%, гейт ПРОЙДЕН (2026-07-28).**
  Инструмент `scripts/measure_accuracy.py` (детерм., без LLM): бинарная дискриминация «подходит/не
  подходит», confusion **TP=19 TN=4 FP=0 FN=0** (0 «принял чужого», 0 «отклонил своего»). Баланс
  выровнен: **19 подходит / 4 не подходит** — специфичность (отклонение чужого товара, ключевая
  ценность продукта) теперь **4/4 = 100%** на 3 разных типах провала (материал / кратность / конструктив).
- [x] Итог: ядро валидно (точность 100% на 23 golden, специфичность 4/4) → можно писать ТЗ на MVP.
- [x] 🚩 **ГЕЙТ ПЕРЕД ПРОДАКШЕНОМ — ЗАКРЫТ (2026-07-28).** Добраны 3 негатива (реальные экспертные «не
  подходит» из журнала), заморожены генератором с фактическим score: `laryngoscope_miller_steel_NEGATIVE`
  (наш Миллер полиамид ↔ ТЗ сталь, материал), `gown_disposable_smms_NEGATIVE` (наш бязь многоразовый ↔
  ТЗ одноразовый SMMS, кратность), `laryngoscope_monolithic_guide_NEGATIVE` (наш сменный световод ↔ ТЗ
  несъёмный монолитный, конструктив). Все 3 → disqualified несмотря на высокий surface-% (85/60/82) —
  hard-нарушение корректно убивает вердикт. test_golden 46/46. ✅ ПОДТВЕРЖДЕНО ФАУНДЕРОМ (2026-07-28):
  все 3 негатива — легитимные «не подходит». Требования реконструированы из журнала (живых ТЗ-файлов
  в git нет — ПДн); при желании позже перезаморозить из реальных ТЗ по id (6a622eea/6a635782/6a62246e).

### ⚪ Этап 1 — MVP на одной категории (1–2 месяца)

- [~] Беспилотный конвейер: **воркер готов** — `scripts/worker.py` (товары компании из БД → карточки →
  `run_auto` → `api/ingest` → свежие лиды в БД); single-shot для cron или `--interval` цикл. Живой смок
  прогнан: воркер нашёл/сматчил/записал лид. `run_auto` сделан устойчивым к лимиту/сбою LLM (не падает
  трейсбеком, останавливается чисто). Осталось: cron-крон и per-товар профиль. ⚠ **БЛОКЕР (внешний):**
  ключ Anthropic исчерпал месячную квоту (`workspace API usage limits`, доступ с 2026-08-01) → живое
  извлечение на паузе до 1 августа; кабинет работает на сид-ленте.
- [~] Старые форматы: `.doc` ✓, `.xls` ✓ (xlrd), **`.zip` ✓** (stdlib, рекурсивный разбор вложенных
  документов, `test_parser_archive.py` 4/4), **`.rar` ~** (rarfile — нужен системный unrar/unar, мягко
  деградирует). PARSE_EXT в 3 скриптах дополнен архивами (+`.doc` в run_auto). Осталось: многопозиц. лоты.
- [~] **Бэкенд FastAPI — КАРКАС + ПРОД-ЛЕНТА (2026-07-29), `api/`:** регистрация+компании, изоляция по
  `company_id`, CRUD товаров, мост `ingest`. **Лента прод-готова:** пагинация (конверт `LeadPage`
  total/limit/offset/items), фильтры (`min_score`, `include_disqualified`, `changed_only`=«требуют
  внимания», `region`, `deadline_max_days`), сортировка (`score`/`deadline`/`updated`) — общий билдер
  `_leads_page`. Новая **СВОДНАЯ лента компании** `GET /leads` по ВСЕМ товарам (сценарий «поставщик с N
  позициями», §9) + per-товар `GET /products/{id}/leads`. `tests/test_api.py` 35→44 (пагинация/фильтры/
  сортировка/сводная/изоляция). Осталось: миграции (alembic), prod-БД (Postgres).
- [~] **Защищённая аутентификация — БАЗА ГОТОВА (secure by design, §5):** argon2id хеш паролей,
  rate-limit логина на (IP+email) с ВРЕМЕННОЙ блокировкой (не вечной), единый ответ «неверный логин
  или пароль» (без утечки существования аккаунта), constant-time сверка при отсутствии юзера, JWT
  из env-секрета, серверная валидация (pydantic). Осталось: **2FA через Telegram-бота**, HTTPS-only
  (терминация на прокси), refresh-токены/ротация, общий стор rate-limit (redis) для многопроцессности.
- [ ] Личный кабинет (React): завёл товар → лента с % и разбором
- [~] **Дозаполнение пробелов клиентом (важно, фаундер) — БЭКЕНД ГОТОВ (2026-07-28):** когда движок
  нашёл конкурс, а карточке не хватает данных (вердикт = пробелы GAP) — клиент вносит значение →
  дописывается в карточку → пересчёт %. Ядро `src/gapfill.py` (`apply_fills` + `recompute` реальным
  матчером; тесты 13/13). API: `GET /products/{id}/leads/{purchase_id}` (разбор по требованиям + список
  пробелов) и `POST …/fill` (значения клиента → карточка status confirmable → пересчёт). Для этого
  `Lead` хранит замороженные требования+синонимы (`requirements_json`/`synonyms_json`), пишет `ingest`
  из `run_auto` (без повторного извлечения → без Anthropic). ЧЕСТНОСТЬ: значение ниже порога не
  «зачитывается» (тест: 50<60 → не проходит). `tests/test_api.py` 16→24. Осталось: UI-поля в кабинете
  (React), опц. миграция БД (+2 колонки; на dev-SQLite пересоздаётся). Текст подсказки — в `Check.action`.
- [~] **Карточка контракта в ленте — БЭКЕНД ГОТОВ (2026-07-28).** Полная обвязка отдаётся API в
  `LeadOut.card`/`LeadDetailOut.card` (модель `ContractCard`): реестровый №, ссылка на ЕИС, заказчик,
  НМЦК, регион, место поставки, способ определения, СМП; **даты** — публикация, начало и **окончание
  подачи** (+рассчитанные `days_to_submission`), **проведение торгов** (`bidding_date`), **подведение
  итогов** (`summing_up_date`); **деньги** — обеспечение заявки, обеспечение исполнения контракта,
  аванс. Путь: `Purchase.contract_card()` (поля добавлены в схему + `_to_purchase` из `fullinfo`) →
  `run_auto` пишет `card` → `Lead.card_json` (ingest) → API. `run_auto` обогащает финалиста из
  `fullinfo` (в лёгком `getlist` части полей нет). Проверено: API-тест 24→30, живой смок `contract_card`
  на реальном `fullinfo` (поля заполняются, где источник не даёт — деградирует к пусто).
  **Срок исполнения контракта/этапов — СДЕЛАНО детерминированным парсером (2026-07-28).** Источник его
  отдельным полем НЕ даёт (живёт в тексте проекта контракта) → `src/contract_terms.py` best-effort по
  типовым формулировкам РФ («в течение 30 календарных дней с даты заключения», «не позднее 31.12.2026»,
  «поэтапно: Этап 1 …») с `confidence` (high/medium/none, не выдумывает). Без LLM (квота-независимо;
  апгрейд до LLM — потом). `run_auto` парсит текст ТЗ/контракта → `purchase.execution_period` → карточка.
  Тесты `test_contract_terms.py` 14/14. Осталось: вывод карточки в UI (React); при квоте — LLM-апгрейд парсера.
- [ ] Доставка: уведомления Telegram / e-mail
- [ ] 3–5 пилотных поставщиков → обратная связь по точности

### ⚪ Этап 2 — Расширение номенклатуры (3–6 мес)

- [ ] Новые категории медизделий/расходников (новые профили, тот же движок)
- [ ] Официальная интеграция ЕИС — второй адаптер источника, уход от Тендерплана
- [ ] Платная подписка, биллинг, самостоятельный онбординг
- [ ] **Самообучение от отыгранных конкурсов (запрос фаундера).** НЕ fine-tuning нейросети —
  накопление структурированного знания (детерминированно, объяснимо для 44-ФЗ, бесплатно, мгновенно):
  (1) выигранный конкурс → авто-golden (регресс + калибровка); (2) авто-дозаполнение карточки
  значениями, которых не хватало (связка с фичей «дозаполнение пробелов», Этап 1); (3) словарь
  синонимов имён по категории копится из подтверждённых связок → align становится детерминированным
  и бесплатным (замена LLM на эмбеддинги, §13; реализация для align_keys — §3.7); (4) калибровка порога/весов на парах «подошёл/не
  подошёл». Оговорки: нужен сигнал подтверждения (expert-in-the-loop, иначе учит ошибки); вычистка
  ПДн из ТЗ; изоляция обогащения по `company_id` (не утекать между поставщиками).
  **МАЙНЕР СИНОНИМОВ ИМЁН — СДЕЛАНО (2026-07-28, п.3 выше):** `src/synonym_miner.py` + CLI
  `scripts/mine_synonyms.py` эмбеддингами (порог 0.85) предлагают кандидатов в `key_synonyms` из имён
  карточек + golden-ТЗ; `suggest_groups` сливает в группы (union-find). БЕЗОПАСНО: только предлагает,
  никогда не применяет — антонимы среди кандидатов (ширина↔толщина, апертура большой↔малый круг, размеры
  клинка _1↔_3) человек отсеивает (expert-in-loop). Тесты `test_synonym_miner.py` 7/7. Живой прогон на
  ларингоскопе (261 имя): нашёл реальные (сервисное_обслуживание↔служба, декларация↔сертификат,
  встроенный_аккумулятор↔аккумулятор, материал_рукоятки↔рукояти) вперемешку с ложными — как и задумано.

### ⚪ Этап 3 — Масштаб и смежное (6–12 мес)

- [ ] 223-ФЗ, коммерческие закупки
- [ ] Апселлы: авто-подготовка заявки, аналитика конкурентов
- [ ] Потенциальная глобализация: TED (ЕС) / SAM.gov (США)

## 5. Ключевые решения (зафиксировано)

- **Ключ Anthropic — только для беспилота.** Нужен, чтобы `extractor.py` крутился в cron
  без человека. Этап 0 при этом НЕ заблокирован: извлекает Claude Code на подписке.
  `export ANTHROPIC_API_KEY=...` (в git не коммитить). Маршрутизация уже заложена (`models.py`).
- **Источник данных для MVP — API Тендерплана** (оплаченный тариф, PAT) как временный
  **заменяемый** источник: быстрый старт без интеграции ЕИС. За интерфейсом `source.py`.
  Тендерплан — конкурент (§7), поэтому осознанно временно; официальная интеграция ЕИС —
  цель прода (второй адаптер), чтобы уйти от вендор-локина. Дифференциатор (извлечение+
  матчинг) от источника не зависит.
- **Мультиарендность — базовое требование** с первого дня (регистрация, компании, изоляция
  товаров по `company_id`). Заложена в модель данных (§11.0).
- **Услуги / работы / НИОКР — ВНЕ продукта.** Требования там прозой, «матчинг по спекам»
  неприменим — это другой продукт. Делаем только поставку товара (Tier A сейчас, Tier B потом).
- **Безопасность by design — с первого дня, не «прикрутим потом».** Аккаунт поставщика = доступ к
  его тендерной аналитике (коммерческая тайна), поэтому аутентификацию проектируем защищённой
  сразу: хеш паролей (argon2/bcrypt, не MD5/SHA), HTTPS-only, серверная валидация всего ввода,
  никакой утечки «существует ли юзер» в ошибках логина. Защита от brute-force — **rate limiting**
  (экспоненциальная задержка + лимит на IP) и **временная** блокировка (~15 мин), НЕ вечный
  lockout после N попыток (иначе злоумышленник блокирует чужие аккаунты — DoS). Главный барьер —
  **2FA / подтверждение входа через Telegram-бота** (второй фактор сильнее любого лимита попыток).
  Секреты (`ANTHROPIC_API_KEY`, `TENDERPLAN_TOKEN`, ключ заказчика) — только в окружении, не в
  коде/git/чате; заказчику — отдельный ключ с лимитом трат в консоли Anthropic.

## 6. Открытые технические долги (в Этап 1, не блокируют Этап 0)

- **Версионность закупки — ДЕТЕКТ ГОТОВ (2026-07-28).** Домен (уточнил фаундер): по 44-ФЗ ТЗ нельзя
  молча переписать — изменение оформляется формальным «изменением извещения», источник фиксирует
  явно (`updateDateTime`/`isChanged`/revision-суффикс), и срок подачи ПРОДЛЕВАЕТСЯ. Реализация
  `src/versioning.py` (тесты 12/12): (1) стабильный порядко-независимый отпечаток требований
  `content_fingerprint`; (2) `classify_change` → **unchanged / formal / material**. Ключевой нюанс:
  НЕ всякое изменение трогает вердикт — отпечаток различает FORMAL (продление срока/формальность →
  вердикт в силе) и MATERIAL (изменились товарные требования → перепроверить). `Lead` хранит
  `content_hash`/`source_updated_at`/`change_status` (липкий, только повышается); `ingest` сравнивает
  прогоны; API отдаёт `change_status`+`change_note` в ленте и детали (api-тест 31→35). Осталось (в
  квоту): собственно ПЕРЕматчинг material-лидов (переизвлечение) + алерт клиенту — срок к тому времени
  продлён, время есть. Уведомление можно слать через будущую Telegram-доставку.
- **Отказоустойчивость извлечения.** Если LLM вернул невалидный JSON или частично вытащил
  таблицу — нужен retry с уточняющим промптом + порог «мало требований → эскалация на Opus».
- **КТРУ-recall на закупках-НАБОРАХ (вскрыто 2026-07-22, тендер 67e1270f).** Заказчик кодирует
  диагностический набор (оториноофтальмоскоп: отоскоп+офтальмоскоп) под ШИРОКИМ/чужим КТРУ
  (`32.50.13` «ОТОСКОПЫ»), а не под КТРУ офтальмоскопа `26.60.12` → `ktru_relation`=`none` →
  движок ложно отсекает релевантную закупку. Нужно: смягчить сверку для родительских кодов
  (префикс-совпадение по классу) ИЛИ keyword-fallback, когда товар — компонент набора. Плюс сам
  матчинг «single-product ↔ ТЗ набора» — товар покрывает лишь часть набора (аналог многолота).
- **Источник требований: характеристики ПОЗИЦИИ, а не общий ТЗ-докс (вскрыто 2026-07-23, халаты 69e0ac8e).**
  Экстрактор из полного 44-ФЗ ТЗ-докса гребёт КОНТРАКТНУЮ ОБВЯЗКУ (сертификат/декларация соответствия,
  разрешение к применению, протокол испытаний, пожарная безопасность, маркировка на русском, гарантия)
  как «требования к товару» → лишние пробелы, заниженный %. На халате: против ТЗ-докса 48%, против
  ХАРАКТЕРИСТИК ПОЗИЦИИ (структурные, из карточки ЕИС/КТРУ) — 100% (7/7, реальные хар-ки товара).
  Вывод: приоритетный источник требований — характеристики позиции (см. §3.3 «2-й источник — КТРУ-базис»),
  а из ТЗ-докса брать только товарные хар-ки, не контрактную обвязку. Нужен парсер характеристик позиции
  (Тендерплан/ЕИС) ИЛИ фильтр «контрактные документарные термины» в экстракторе.
- **`zakupki.mos.ru` — НЕ CA-бандл, а ГОСТ-крипто (пробито живьём 2026-07-28, переформулировано).**
  Разведка вскрыла: задача НЕ решается добавлением сертификата. Цепочка mos.ru ЦЕЛИКОМ на ГОСТ:
  лист (ДИТ Москвы) ← «Федеральное казначейство» (sub-CA) ← **ГУЦ 2022** (корень Минцифры, ОТЛИЧНЫЙ
  от Russian Trusted Root CA), подпись везде **GOST R 34.10-2012**. Для сравнения zakupki.gov.ru — RSA
  (работает). Последствия: (1) `cryptography`/OpenSSL Python'а ГОСТ НЕ умеют → ни проверить подпись, ни
  сделать ГОСТ-рукопожатие даже с корнем в бандле; (2) корень ГУЦ 2022 из этой среды не тянется
  (`crl.gosuslugi.ru` таймаут, зеркала 403/404). ВЫВОД: поддержка mos.ru = отдельный инфра-эпик (нужен
  `gost-engine` для OpenSSL, либо КриптоПро CSP, либо ГОСТ-совместимый загрузчик), а НЕ правка
  `setup_ca_bundle.py`. **Приоритет низкий:** федеральный zakupki.gov.ru (RSA) покрывает большинство
  тендеров и работает; московский региональный — подмножество. Отложено сознательно до ГОСТ-стека.

---

---

# ЧАСТЬ II. СПРАВКА (как устроено)

## 7. Идея и отличие от конкурентов

Поставщик заводит свою номенклатуру с техническими характеристиками. Система сама находит
закупки, читает **текст ТЗ каждой** и выдаёт вердикт: **проходит / не проходит / нужно
подтвердить**, с процентом соответствия и разбором по каждому требованию.

**Отличие от конкурентов (Seldon, Тендерплан, Контур):** они матчат по классификаторам
(ОКПД2 / КТРУ) и ключевым словам. Мы матчим по **фактическим характеристикам из ТЗ** —
толщина ≥0,11 мм, размеры S/M/L, тип отделки, — которых в классификаторах нет. Именно там
заказчики по ст. 33 44-ФЗ пишут требования руками, и классификаторный поиск слеп.

## 8. Сквозной принцип архитектуры

> **Можно сделать кодом — делай кодом. LLM — только там, где нужен «разум».**

LLM НЕ читает каждую закупку целиком. Дорогой «разум» подключается хирургически и только к
финалистам. Основную воронку прожимает дешёвый детерминированный код + эмбеддинги. Это
держит себестоимость на копейках при любом масштабе. Воронка сужается на каждом шаге:

```
Все закупки (сотни тыс./день)
   │  [код: фильтр по ОКПД2/КТРУ/ключевым словам]
   ▼
Грубые кандидаты (сотни/день на поставщика)
   │  [эмбеддинги: семантический отсев]
   ▼
Кандидаты на глубокий разбор (десятки/день)
   │  [код: скачать вложения ТЗ]
   ▼
ТЗ извлечены в текст (+ OCR сканов)
   │  [LLM: вытащить структурированные требования]
   ▼
Требования в JSON
   │  [код + LLM: матчинг с карточкой товара]
   ▼
Вердикт: % соответствия + статусы по каждому требованию
   │  [LLM: человекочитаемое объяснение]
   ▼
Уведомление поставщику
```

## 9. Конвейер по шагам: кто, что, чем

### Шаг 0. Онбординг поставщика (разово)

- **Что:** поставщик заводит карточки товаров с характеристиками (форма или импорт из Excel/1С).
- **Кто:** фронтенд + код. LLM-помощник **опционально** нормализует «грязный» прайс в структуру.
- **LLM:** Haiku 4.5 — разбор прайса в JSON (разово, дёшево). **Выход:** карточки товаров (§11.1).

### Шаг 1. Сбор закупок (discovery)

- **Что:** непрерывно тянем новые/изменённые закупки. **Источники:** ЕИС / ЭТП (сейчас Тендерплан).
- **Кто:** **код** (планировщик + загрузчик), инкрементально по дате публикации. **LLM:** нет.
- **Выход:** сырые карточки закупок (реестр, ОКПД2/КТРУ, НМЦК, заказчик, ссылки на вложения).

### Шаг 2. Грубые фильтры воронки (дёшево → дорого) ← дешёвый код, отсекает 99%

- **Что:** оставить только релевантные и **актуальные** закупки до дорогого анализа ТЗ.
- **Кто:** **код** (`filter.py`), по порядку:
  1. **КТРУ / ОКПД2** (обязательное поле поставщика; список кодов, матч по префиксу);
  2. **Срок подачи** — окно дней до дедлайна (напр. 1–29): только активные, где успеваешь подать;
  3. **Регион** — вся РФ или выбранные коды регионов (где поставщик возит).
- **LLM (опционально):** Haiku 4.5 — расширение запроса синонимами, редко (при настройке профиля).
- **Сборные лоты:** многопозиционный тендер (товар — 1 из N позиций) КТРУ-фильтр проходит и **не
  отсекается**; переключатель `lot_mode` (solo / any) — см. §11.4. Матчинг — по конкретной позиции.
- **Выход:** короткий пул кандидатов → в глубокий анализ (шаги 4–8).

### Шаг 3. Семантический отсев (coarse matching)

- **Что:** отсеять явно нерелевантное до дорогого разбора («перчатки боксёрские» ≠ «медицинские»).
- **Кто:** **эмбеддинги** (векторное сходство «предмет закупки» ↔ «товар»), быстро и почти бесплатно.
- **Модель:** мультиязычный эмбеддер под русский (bge-m3 / multilingual-e5) + pgvector.
- **СТАТУС (2026-07-28): МОДУЛЬ + ТЕСТЫ ГОТОВЫ (`src/semantic_filter.py`).** `product_text` (название +
  идентиф. характеристики + extra-контекст) ↔ `subject_text`; `rank` (по убыванию сходства, ничего не
  теряет) + `filter_relevant` (порог, опционально); та же инъекция `embedder` + мягкая деградация без
  модели. Тесты `tests/test_semantic_filter.py` 12/12 (FakeEmbedder). ЖИВОЙ смок bge-m3 доказал подхват:
  «Устройство для видеоассистированной интубации трахеи» (НИ слова «ларингоскоп»/«клинок») = 0.63,
  выше смежного «аппарат ИВЛ» 0.52 и чужих «перчаток» 0.33 — то, что keyword-фильтр пропустил бы.
  **КАЛИБРОВКА НА ЖИВОМ ФИДЕ (2026-07-28, Tenderplan-токен) — drop-фильтр НЕ ГОДИТСЯ на предметах РФ.**
  Замер: релевантные ларингоскоп-закупки 0.535–0.707 vs чужие медизделия фида 0.49–0.565 — ПЕРЕКРЫТИЕ
  полное («Канистра для аспирации»=0.565 и «Маска ларингеальная»=0.553 выше реального «Поставка
  ларингоскопов с принадлежностями»=0.535). Причина: предметы закупок РФ ОБОБЩЁННЫЕ («Поставка изделий
  мед. назначения (…)») — различительный сигнал спрятан в скобках/ТЗ, не в заголовке; на generic-строке
  эмбеддинг видит всё как «медизделия для операционной». Чистого порога нет. ВЫВОД: семантический отсев
  как drop-фильтр воронки на предметах РФ НЕ работает; точный сигнал — КТРУ/ОКПД2-коды + ключевые слова
  (`filter.py`/`ktru.py`, уже в воронке, точнее семантики на generic-заголовках). В `run_auto` НЕ встраиваем.
  Модуль `semantic_filter.py` остаётся (протестирован) для задач, где текст БОГАТЫЙ (не generic-заголовок):
  кандидат — RAG «похожие прошлые конкурсы» по тексту ТЗ, дедуп, ранжирование внутри уже-релевантных.
  Урок (в скилл `embed-calibrate`): калибруй на ЖИВЫХ предметах — на синтетике «перчатки vs клинок»
  разделялось (0.33 vs 0.7), на реальных generic-предметах — нет.

### Шаг 4. Забор ТЗ (fetch)

- **Что:** скачать вложения (описание объекта, проект контракта): .docx / .pdf / .xls / сканы.
- **Кто:** **код** (загрузчик из ЕИС/ЭТП). **LLM:** нет. **Выход:** файлы ТЗ по каждому кандидату.

### Шаг 5. Извлечение текста ТЗ (parse + OCR)

- **Что:** документ → чистый текст/таблицы.
- **Кто:** .docx/.xlsx/текстовый .pdf → **код** (python-docx, openpyxl, pdfplumber); скан → **OCR**.
- **OCR:** Tesseract (rus) на старте; кривые сканы — vision-модель Claude как fallback.

### Шаг 6. Извлечение требований (structured extraction) ← сердце №1

- **Что:** из текста ТЗ — список требований в строгий JSON: характеристика, оператор
  (=, ≥, ≤, диапазон, «одно из»), значение, единица, жёсткость («не изменяется»=дисквалификатор /
  «участник указывает»=мягкое), документарные требования (РУ, срок годности).
- **Кто:** **LLM** со строгой JSON-схемой. **Модель:** **Sonnet 5** (Haiku — простые, Opus 4.8 — эскалация).

### Шаг 7. Матчинг товар ↔ требования ← сердце №2

- **Что:** сверить каждое требование с карточкой, присвоить статус.
- **Кто:** гибрид: **код** — числа и логика операторов (0,11 ≥ 0,11, наборы, пороги, дисквалификаторы);
  **LLM** — только семантическая эквивалентность формулировок, точечно.
- **Выход:** по требованию статус ✅ проходит / ❌ нарушение / ⚠️ пробел.

### Шаг 8. Вердикт и скоринг

- **Кто:** **код** — расчёт % и статуса по правилам (§12); **LLM** — текст объяснения поставщику.
- **Модель:** Opus 4.8 (понятный русский вердикт). **Выход:** карточка результата (§11.3).

### Шаг 9. Доставка и лента результатов

- **Что:** лента в кабинете + алерты (Telegram / e-mail). **Кто:** **код**. **LLM:** нет.
- **Сортировка ленты (решено):** по **% совпадения по убыванию** (100% сверху → вниз).
  Сверху — где проходишь целиком, ниже — с пробелами (`eligible_with_gaps`).
- **Дисквалифицированные** (`disqualified`, нарушено обязательное требование) — **не в основной
  ленте**, а отдельной свёрнутой секцией «не подходит».
- **Вторичная сортировка при равном %:** по сроку подачи (срочные выше) — не упустить закрывающиеся.
- Каждая строка ленты = вердикт (% + разбор) + карточка контракта (§4 Этап 1) + пометка
  «позиция N из M» для сборных лотов (§11.4).

## 10. Матрица LLM: какая модель за какой запрос

| Шаг | Задача                                                         | Исполнитель | Модель                                            | Частота                           |
| ------ | -------------------------------------------------------------------- | ---------------------- | ------------------------------------------------------- | ---------------------------------------- |
| 0      | Разбор прайса поставщика в JSON               | LLM                    | Haiku 4.5                                               | разово на онбординге   |
| 1      | Сбор закупок                                              | код                 | —                                                      | постоянно                       |
| 2      | Фильтр по ОКПД2/ключевикам                     | код                 | —                                                      | постоянно                       |
| 2      | Расширение запроса синонимами             | LLM                    | Haiku 4.5                                               | при настройке профиля |
| 3      | Семантический отсев                                | эмбеддинги   | bge-m3 / e5 (RU)                                        | на каждого кандидата   |
| 4–5   | Скачать + парсить ТЗ                                 | код                 | —                                                      | на финалистов                |
| 5      | OCR сканов                                                     | OCR/vision             | Tesseract (rus) → Claude vision                        | только сканы                  |
| 6      | **Извлечение требований**                  | LLM                    | **Sonnet 5** (Haiku/Opus по сложности) | на финалистов                |
| 7      | Числовой/логический матчинг                 | код                 | —                                                      | на финалистов                |
| 7      | Семантическая эквивалентность полей | LLM/эмбеддинги | align_keys → bge-m3 (§3.7); align_values → Haiku 4.5 | точечно                           |
| 8      | Расчёт % и статуса                                     | код                 | —                                                      | на финалистов                |
| 8      | **Текст вердикта**                                | LLM                    | **Opus 4.8** (резюме)                       | на финалистов                |
| 9      | Уведомления                                               | код                 | —                                                      | постоянно                       |

**Принцип выбора (реализовано в `src/models.py`):** Haiku — массовые простые операции;
Sonnet 5 — извлечение требований; Opus 4.8 — резюме/вердикт и эскалация спорных ТЗ;
эмбеддинги — вся грубая семантика воронки вместо генеративных вызовов.

## 11. Модели данных

> **Мультиарендность заложена с первого дня.** Иерархия: `Company` (арендатор) → `User`
> (роли) → `Product[]` → `MatchResult[]`. Закупки (`Purchase`) и требования — **общие** для
> всех арендаторов (публичные данные ЕИС); товары и результаты изолированы по `company_id`.

### 11.0. Компания и пользователь (`Company`, `User`)

```json
{ "id": "co_acme", "name": "ООО «Медснаб»", "inn": "7701234567",
  "plan": "pilot", "created_at": "2026-07-20" }

{ "id": "usr_01", "company_id": "co_acme", "email": "buyer@medsnab.ru",
  "role": "owner", "created_at": "2026-07-20" }
```

Изоляция: каждая строка `Product`/`MatchResult`/профиля несёт `company_id`; любой запрос к
товарным данным фильтруется по `company_id` (row-level, не только UI). Регистрация: завёл
компанию (`owner`) либо приглашён (`member`).

### 11.1. Карточка товара (`Product`)

```json
{
  "id": "gloves-nitrile-blue",
  "company_id": "co_acme",
  "name": "Перчатки смотровые нитриловые, неопудренные, нестерильные",
  "attributes": [
    {"key": "материал", "value": "нитрил", "critical": true},
    {"key": "стерильность", "value": "нет"},
    {"key": "опудренность", "value": "нет"},
    {"key": "размеры", "value": ["XS","S","M","L"], "status": "declared"},
    {"key": "толщина_пальцы_мм", "value": 0.11, "status": "confirmable", "doc": "протокол испытаний"}
  ],
  "documents": ["регистрационное_удостоверение"]
}
```

Три статуса данных: `declared` (в карточке), `confirmable` (подтверждается документом), `gap` (нет).

### 11.2. Требование закупки (`Requirement`)

```json
{
  "key": "толщина_пальцы_мм",
  "raw": "Одинарная толщина (в области пальцев) ≥ 0,11 мм",
  "operator": "gte",        // eq | gte | lte | range | one_of | set | present
  "value": 0.11, "unit": "мм",
  "hardness": "soft",       // hard = дисквалификатор | soft = участник указывает
  "type": "technical"        // technical | documentary
}
```

### 11.3. Результат сопоставления (`MatchResult`)

```json
{
  "purchase_id": "...", "company_id": "co_acme", "product_id": "gloves-nitrile-blue",
  "score": 92, "verdict": "eligible_with_gaps",   // eligible | eligible_with_gaps | disqualified
  "checks": [
    {"req": "материал=нитрил", "status": "pass"},
    {"req": "размеры S,M,L", "status": "gap", "action": "подтвердить наличие S/M/L"}
  ],
  "explanation": "Проходишь по всем техническим характеристикам. Закрой пробел: размеры."
}
```

### 11.4. Карточка поставщика: онбординг, настройки поиска, сборные лоты

**Иерархия:** `Company` → `Product[]` (+ настройки поиска). Каждый товар матчится независимо,
лента результатов агрегирует по компании.

**Поля карточки товара:**

| Поле               | Что                                                                                             | Роль                                                           |
| ---------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `name`               | название товара                                                                      | отображение                                             |
| **`ktru[]`**   | **обязательно** — коды КТРУ/ОКПД2 (список)                     | якорь воронки (шаг 2)                               |
| `category`           | выводится из КТРУ → профиль категории                              | связь со справочником                           |
| `attributes[]`       | характеристики`{key, value, status, doc}`                                          | сердце матчинга (§11.1)                             |
| `documents[]`        | какие документы поставщик даёт (РУ №, серт., протоколы) | закрывают документарные требования |
| `delivery_regions[]` | коды регионов поставки                                                         | фильтр воронки (шаг 2)                             |
| `search{}`           | настройки поиска (ниже)                                                         | параметры воронки                                  |

**Онбординг — заводить карточку можно И руками, И автоматически (оба способа в MVP):**
(1) **форма по категории** — поля подсказывает профиль КТРУ (поставщик заполняет вручную);
(2) **загрузка спеки/ТЗ товара** (docx/pdf/фото) → LLM извлекает характеристики (тот же extractor,
развёрнутый на товар) → поставщик проверяет/правит форму. Плюс импорт Excel/1С (Haiku нормализует).
Если поставщик не знает КТРУ — LLM подсказывает по названию.

**Настройки поиска (`search`) — на КАЖДЫЙ товар отдельно** (решено): регионы поставки, окно срока
подачи (дней), min/max НМЦК, **режим лота** `lot_mode`. Каждый товар ищется по своим настройкам.

**Сборные лоты (важно — не терять):** тендер бывает многопозиционным (напр. 5 позиций, товар —
одна из них). В РФ лот — «всё или ничего». Переключатель `search.lot_mode`:

- `solo` — только чистые лоты (товар = вся/основная позиция, подаёшься напрямую);
- **`any` (дефолт)** — включая сборные, **не отсекаем**, с пометкой «позиция N из M» + что ещё в лоте.

Движок: многопозиционный тендер проходит КТРУ-фильтр (совпал по одной позиции — не выкидываем);
**матчинг идёт по конкретной позиции** (её КТРУ = товар), не по всему лоту. В карточке результата —
«твой товар = позиция N из M, остальное в лоте: …». Данные по позициям уже есть в источнике
(Тендерплан `fullinfo`, таблица `ObjectInfo.Objects`: Name/КТРУ/Quantity/Price на позицию).

**Ключевой инсайт:** карточка товара и требование закупки — одна структура `{key, operator, value}`;
матчинг сводит их. Профиль категории (от КТРУ) задаёт словарь ключей и синонимов
(ISO 7376 ⇔ Flexline, полиамид ⇔ пластик) — товар это конкретика, профиль общий справочник.

## 12. Логика скоринга

1. **Дисквалификаторы (hard).** Нарушено `hard`-требование → `disqualified`, процент не важен.
2. **Пробелы (gap).** Требование есть, данных в карточке нет → не «−балл», а флаг «подтвердить».
   Это ценность продукта, а не ошибка.
3. **Процент** = взвешенная доля выполненных требований (`technical` весит больше `documentary`).
4. **Статусы:** `eligible` (всё pass) · `eligible_with_gaps` (нет нарушений, есть пробелы) ·
   `disqualified` (нарушение hard).

## 13. Технологический стек (MVP)

| Слой                      | Выбор                                                           | Комментарий                                                                      |
| ----------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Ядро / скрипты     | Python 3                                                             | детерминированная логика                                             |
| Хранилище            | PostgreSQL + pgvector                                                | закупки, требования, товары, векторы                          |
| Парсинг                | python-docx, pdfplumber, openpyxl                                    | текст+таблицы                                                                   |
| OCR                           | Tesseract (rus) → Claude vision                                     | сканы                                                                                  |
| Эмбеддинги          | bge-m3 / multilingual-e5 (self-hosted)                               | нужен сервер под инференс; заменяют Haiku в `align_keys` (§3.7)                           |
| LLM                           | Claude API (Haiku 4.5 / Sonnet 5 / Opus 4.8)                         | извлечение, вердикты                                                      |
| Источник данных | Тендерплан (сейчас) → офиц. ЕИС (позже) | за интерфейсом`source.py`                                                    |
| Бэкенд                  | FastAPI (Python)                                                     | аутентификация + изоляция по`company_id` с первого дня |
| Фронтенд              | React (личный кабинет, лента)                      | позже                                                                                  |
| Доставка              | Telegram Bot API + e-mail                                            | алерты                                                                                |
| Хостинг                | Railway / VPS                                                        | быстрый старт                                                                   |

## 14. Генерализация: профили категорий

**Движок не привязан к перчаткам.** Ядро (извлечение → матчинг → скоринг) category-agnostic:
карточка и требование — произвольные `{key, operator, value, unit}`, логика работает на
операторах (`≥ ≤ = one_of set semantic`), которым всё равно, `0,11 ≥ 0,11` это или `5,5 кВт ≥ 4`.
Переход на другой товар **не требует нового кода** — меняется только профиль.

**Профиль категории (данные, не код):** коды ОКПД2/КТРУ, ключевые слова/синонимы, словарь
характеристик, единицы, критические характеристики (дисквалификаторы), обязательные документы.

**Онбординг новой категории:** взять 5–10 реальных ТЗ → Opus 4.8 предлагает черновик профиля →
эксперт валидирует (~30–60 мин) → прогон 20–30 ТЗ на точность → в прод. **~1–2 дня, не месяцы.**

**Уровни сложности:**

| Уровень                                                | Категории                                                                                               | Статус              |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------- |
| 🟢**Tier A** — товары со spec-таблицей | перчатки, расходники, канцелярия, стройматериалы, реагенты     | **в работе** |
| 🟡 Tier B — оборудование, медтехника   | десятки характеристик, ссылки на ГОСТ, больше эскалаций на Opus | задел                |

Услуги/работы/НИОКР — вне продукта (см. §5).

## 15. Экономика (ориентир)

**Себестоимость на закупку-финалист:** эмбеддинги/фильтр ~0 ₽; извлечение (Sonnet 5) + OCR +
вердикт — единицы рублей каждое. Десятки финалистов/день ⇒ **десятки–сотни ₽/день на поставщика**.

**Монетизация:** дорогая B2B-подписка (тендеры = прямые деньги поставщика). Ориентир —
15–50 тыс. ₽/мес в зависимости от числа категорий/товаров. Маржа высокая: costs на порядок ниже чека.

### 15.1. Масштабирование (расчёт, 2026-07-28)

**Две РАЗНЫЕ оси стоимости — не путать:**
- **Матчинг (товар↔требования) — КОД, ~0 ₽, масштабируется тривиально.** `matcher.py` без LLM,
  микросекунды. 15 товаров у клиента или 10 000 — не имеет значения. **Число товаров/клиентов на этой
  оси не стоит ничего.**
- **Извлечение ТЗ (`extractor`, Sonnet) — единственная реальная стоимость (~12 ₽/ТЗ, замер §2).**

**Ключевой инсайт — извлечение амортизируется, стоимость растёт НЕ с числом клиентов:**
1. Требования тендера **ОБЩИЕ для всех арендаторов** (§11: закупки/требования общие, изолированы
   только товары/результаты по `company_id`) → ТЗ извлекается ОДИН раз, переиспользуется всеми
   клиентами категории.
2. Извлечение **кэшируется** (`run_auto`: индекс→требования); повторно только НОВЫЕ тендеры.
3. Дешёвый код-фильтр режет 99% ДО LLM (§8) → извлечение видит только десятки финалистов/день.

**Модель (порядок величины).** Стоимость ≈ (уникальные тендеры категорий в системе) × 12 ₽, НЕ ×
клиентов. Пусть рынок даёт ~200 новых релевантных финалистов/день по обслуживаемым категориям →
~72 000 ₽/мес извлечения НА ВСЮ систему:

| Клиентов (в тех же категориях) | Cost извлечения/мес (общий) | Cost на клиента | Выручка @15к₽ | Валовая маржа |
|---|---|---|---|---|
| 10   | ~72 000 ₽ (фикс)  | ~7 200 ₽ | 150 000 ₽ | ~52% |
| 100  | ~72 000 ₽ (фикс)  | ~720 ₽   | 1 500 000 ₽ | ~95% |
| 1000 | ~72 000 ₽ (фикс)  | ~72 ₽    | 15 000 000 ₽ | ~99.5% |

Инсайт: рост клиентов В СУЩЕСТВУЮЩИХ категориях — почти чистая маржа (извлечения общие). Рост в НОВЫХ
категориях добавляет извлечения (но в пределах категории — тоже общие). Стоимость масштабируется с
числом уникальных тендеров, не клиентов.

**Реальные узкие места (не матчинг!):** (1) rate-limit/квота Anthropic — митигейт: prompt caching,
**Batch API** (−50%, async), выше тариф, амортизация/кэш; (2) повторное извлечение при смене ТЗ →
версионность (техдолг §6); (3) OCR сканов (CPU) → воркеры; (4) инференс эмбеддингов → 1 GPU/CPU-сервер.

**⚠ Это архитектурный расчёт + замер стоимости одного ТЗ, НЕ нагрузочный тест.** Перед продом измерить:
финалистов/клиента/день (счёт за извлечение), медиану стоимости ТЗ (3–десятки ₽), долю кэш-хитов
(переиспользование тендеров между клиентами).

## 16. Риски и как крыть

| Риск                                                                                                   | Митигейт                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ТЗ в сканах/кривых PDF                                                                      | OCR + vision-fallback; на MVP категории с «текстовыми» ТЗ                                                                                                                    |
| Точность извлечения требований                                                 | строгая JSON-схема, эскалация на Opus, golden-набор для регрессии                                                                                              |
| Ложные вердикты (доверие)                                                             | всегда показывать разбор + пометку «проверьте пробелы», не «чёрный ящик»                                                                  |
| Изменения структуры ЕИС/ЭТП                                                        | абстракция источника (`source.py`); парсер отдельно от ядра                                                                                                   |
| Конкуренты допилят глубокий матч                                              | скорость + накопленная база размеченных ТЗ = ров                                                                                                                |
| Себестоимость LLM при росте                                                           | максимум логики в код и эмбеддинги; LLM только на финалистов                                                                                            |
| Взлом аккаунта поставщика (brute-force, утечка данных тендеров) | secure by design (§5): хеш паролей, rate limiting + временный lockout, 2FA через Telegram, серверная валидация                                              |
| DoS через блокировку чужих аккаунтов                                          | НЕ вечный lockout после N попыток — временная блокировка + rate limit на IP; вход защищает 2FA, а не жёсткий лимит попыток |
