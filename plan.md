# План: Лекало — площадка поиска торгов

> Статус и дорожная карта. Операционка (как собрать/задеплоить, эндпоинты, грабли) —
> в [`CLAUDE.md`](CLAUDE.md). Версия 1.0 · переписан 2026-07-23 под новый замысел.
> Легенда: `[x]` сделано · `[~]` частично · `[ ]` нет · ⭐ приоритет.

## 1. Что это

Собственная площадка поиска госзакупок (44/223-ФЗ) — аналог Тендерплана/Контура, но
**независимая**: свой сбор данных напрямую с площадок. Поиск по ключевым словам, лента
закупок со всех источников, фильтры, скачивание документов. Дифференциатор сверху —
**сверка своего ТЗ с ТЗ закупки** (надстройка).

**Разворот (было→стало):** проект начинался как «SpecMatch» — надстройка на API
Тендерплана (подбор закупок под товар по характеристикам ТЗ). Ушли от зависимости от
конкурента к своей площадке со своим сбором данных.

## 2. Где мы сейчас — РАБОТАЕТ И ЗАДЕПЛОЕНО

Живой сайт: **http://186.246.30.213/** (заменил Nexara на том же VPS).

- [x] Фронт-витрина: поиск по ключевым словам (стемминг + минус-слова), лента карточек,
  **пагинация** «Показать ещё».
- [x] Фильтры: заказчик, регион, срок поставки, сумма (НМЦК), закон (44/223), этап, площадка.
  Регион/площадка — из реальных данных.
- [x] Карточка: позиции, сроки, обеспечение, **скачивание документов** (прямые ссылки).
- [x] **Живое устаревание**: просроченные по сроку подачи уходят из ленты сами (+ раз в минуту).
- [x] Два источника, единая лента: **Портал поставщиков (mos.ru)** + **ЕИС (zakupki.gov.ru)**.
- [x] **~1000 закупок** в снапшоте (Портал ~400 + ЕИС ~600), сбор ~1 мин (конкурентно).
- [x] Автообновление: Планировщик Windows раз в час → пересбор → деплой при смене состава.
- [x] Сборщики за единым интерфейсом (`tools/sources/*`) — новая площадка = новый модуль.
- [x] FastAPI-бэкенд написан (`server/`) — на будущее (в проде не запущен, см. §4).

## 3. ⭐ Прямо сейчас (ближайшее)

1. **Регион для ЕИС** — сейчас пуст, фильтр по региону для ЕИС не работает (они под «Вся РФ»).
   Достать из HTML-выдачи/карточки. Портал регион уже отдаёт.
2. **Git-рост снапшота** — 2.1 МБ коммитится ежечасно. Решить: оставить, или доставлять снапшот
   на VPS через `scp` (убрать git-шум).
3. **Домен + HTTPS** на VPS (сейчас голый HTTP по IP).

## 4. Дорожная карта

### Данные и охват
- [ ] Регион ЕИС (см. §3).
- [~] Больше площадок за тем же интерфейсом. **Разведка (2026-07-23):**
  - **РТС / Фабрикант / Газпром — НЕ берутся автосбором.** Даже настоящим headless-Chromium
    (Playwright) реальные домены РТС (`rts-tender.ru`, `market.rts-tender.ru`, `223.rts-tender.ru`)
    отдают `ERR_CONNECTION_CLOSED`/`ERR_TIMED_OUT`; `etpgpb.ru` не пускает; Фабрикант нестабилен/000.
    Блок на уровне соединения/сети, не JS-челлендж. **NB:** `rts-market.ru` — чужой припаркованный
    домен (Авиасейлс), не площадка РТС. Их регулируемые 44/223 всё равно есть в ЕИС.
  - Реальные пути к ним: официальный API/аккредитация у площадки (не код), либо residential-прокси +
    браузер (дорого, хрупко, ToS-риск). ТЭК-Торг — не проверяли.
- [ ] Больше объёма (тюнинг env), если надо — но статика тяжелеет; см. «Полный реестр».

### ТЗ-сверка (дифференциатор)
- [~] Движок сверки написан в `server/matching.py` (детерминированный: парсинг docx/pdf/xlsx +
  покрытие требований). На фронте надстройка есть (тумблер), но на снапшоте нет данных `matches`.
- [ ] Оживить: загрузка своего ТЗ → сверка с ТЗ закупки. Требует живого бэкенда (см. ниже) или
  клиентского парсинга. Позже — LLM-извлечение требований (Sonnet) вместо эвристики.

### Полный реестр вживую (следующий уровень архитектуры)
- [ ] **БД-бэкенд** (FastAPI + Postgres) на хостинге **с доступом к источникам** (текущий VPS
  заблокирован гос-сайтами!). Тогда: весь реестр (десятки тыс.), серверный поиск/фильтры,
  пагинация по API, документы/сверка по запросу. Варианты доступа: другой хост с «чистым» IP
  или прокси/релей через жилой IP.

### Продукт / прод
- [ ] Домен + HTTPS (Let's Encrypt).
- [ ] Личные кабинеты, регистрация компаний, сохранённые поиски на сервере (сейчас — localStorage).
- [ ] Уведомления (Telegram/e-mail) о новых подходящих закупках.
- [ ] Безопасность по-взрослому (хеш паролей argon2/bcrypt, rate-limit, 2FA) — когда появятся аккаунты.

## 5. Зафиксированные решения

- **Источник — свой сбор напрямую** (не агрегатор). Портал поставщиков (чистый JSON) и ЕИС
  (HTML/RSS) вскрыты и работают анонимно.
- **Сбор — на машине пользователя**, т.к. гос-сайты блокируют IP дата-центров (VPS не видит источники).
- **Прод — статический снапшот на VPS** (быстро, дёшево, без живого бэкенда). Живой бэкенд — цель
  следующего уровня, упирается в доступ хоста к источникам.
- **Скачивание файлов — прямыми ссылками** из браузера пользователя (CORS не мешает навигации).
- **Nexara — отдельный проект**, его заменили на этом VPS (бэкап снят).

## 6. Открытые вопросы

- Где взять хост с доступом к mos.ru/zakupki.gov.ru для живого бэкенда (или ставить прокси)?
- Оставлять снапшот в git или доставлять `scp`-ом?
- Приоритет площадок: сначала регион ЕИС и стабилизация, или новые площадки?
