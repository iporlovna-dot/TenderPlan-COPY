# Парсинг документов ТЗ (шаг 5)
python-docx>=1.1.0
pdfplumber>=0.11.0
openpyxl>=3.1.0
# LLM-извлечение требований и вердикты (шаги 6, 8) — провайдер по LK_LLM_PROVIDER,
# см. src/llmclient.py (по умолчанию DeepSeek, Anthropic доступен флагом)
anthropic>=0.40.0
openai>=1.40.0
# Локальные эмбеддинги: семантика воронки без облака (шаг 3 + align_keys, plan §3.7/§13).
# Тянет torch (~тяжёлый); модель bge-m3 скачивается при первом запуске. Не требуется для
# юнит-тестов keymatch (там инъекция FakeEmbedder); если пакет не установлен — align_keys
# мягко деградирует на Haiku.
sentence-transformers>=3.0.0
# HTTP-клиент для источника закупок (Тендерплан, шаги 1, 4)
httpx>=0.27.0
# OCR сканов-PDF (шаг 5): рендер страниц + распознавание
pymupdf>=1.24.0
easyocr>=1.7.0          # pip-only OCR с русским (fallback, если нет системного tesseract)
pytesseract>=0.3.10     # обёртка к системному tesseract (если он установлен — предпочтительнее)
# MVP-бэкенд (Этап 1): API «товар → лента», мультиарендность, защищённая аутентификация
fastapi>=0.110.0
uvicorn>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13          # миграции схемы БД (версионирование, prod-редеплой без потери данных)
psycopg2-binary>=2.9   # драйвер Postgres (prod-БД; URL через SPECMATCH_DATABASE_URL=postgresql://…)
redis>=5.0             # опц. общий стор rate-limit логина для многопроцессности (SPECMATCH_REDIS_URL)
pydantic-settings>=2.2.0
argon2-cffi>=23.1.0     # хеш паролей (secure by design)
pyjwt>=2.8.0            # токены сессии
email-validator>=2.0.0
xlrd>=2.0.1          # чтение старого бинарного .xls (openpyxl не умеет)
rarfile>=4.0            # распаковка .rar (нужен системный unrar/unar); .zip — stdlib
