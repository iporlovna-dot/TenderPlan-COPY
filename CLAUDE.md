# CLAUDE.md — Лекало (поиск торгов)

> Операционная памятка проекта. Читаю в начале каждой сессии. Дорожная карта и
> статус — в [`plan.md`](plan.md). Здесь — что это, как устроено, как обновлять
> данные и деплоить, и грабли, на которые уже наступали.

**Лекало** — собственная площадка поиска госзакупок (аналог Тендерплана/Контура):
поиск по ключевым словам, лента закупок 44/223-ФЗ со всех площадок, фильтры,
скачивание документов. Сверху — дифференциатор: сверка своего ТЗ с ТЗ закупки
(надстройка). Замысел: **независимость от чужих агрегаторов** — свой сбор данных.

> ⚠️ **Историческая справка.** Раньше проект назывался «SpecMatch» и строился как
> надстройка на API Тендерплана. От этого ушли. Если встретишь в старых заметках
> «SpecMatch / Тендерплан как источник / src/matcher.py» — это устарело.

---

## Архитектура (как есть сейчас)

**Статический снапшот + статический фронт.** Никакого живого бэкенда в проде:

```
Node-сборщики (tools/) → site/data/purchases.json → статический фронт (site/) → nginx на VPS
        ↑ гоняются на машине пользователя (гос-сайты блокируют IP дата-центров)
```

- **Фронт** (`site/`) — чистый HTML/CSS/JS, состояние в localStorage. Грузит
  `data/purchases.json` и рендерит ленту с поиском/фильтрами/пагинацией. ТЗ-сверка —
  надстройка (тумблер), на снапшоте пока без данных `matches`.
- **Сборщики** (`tools/`) — тянут закупки с площадок, нормализуют в единую схему,
  копят их в **накопителе** и пишут `site/data/purchases.json`.
- **Накопитель** (`tools/.cache/store.json`) — состояние сборщика между прогонами:
  закупка живёт в нём до истечения срока подачи, окно ЕИС только доливает новое.
  Снапшот — срез накопителя, а не результат одного прогона (см. «Накопитель» ниже).
- **Бэкенд** (`server/`, FastAPI) — **написан, но в проде НЕ работает**: VPS
  заблокирован источниками (см. грабли). Оставлен на будущее (БД-бэкенд).

### Карта файлов

| Путь | Что |
|---|---|
| `site/index.html` | лендинг · `site/app.html` — приложение (лента) · `login/register.html` |
| `site/js/config.js` | `window.LK_API_BASE` — "" = статический снапшот; URL = живой API |
| `site/js/app.js` | стор `LK` (localStorage), моки-фолбэк, `loadPurchases()` (API→снапшот→моки) |
| `site/js/appview.js` | лента, поиск, фильтры, **пагинация**, живое устаревание, ТЗ-надстройка |
| `site/js/tzmatch.js` | термины/сравнение ТЗ + разбор .docx. **Общий для браузера и Node** (см. «Умная сверка») |
| `tools/sources/tzterms.js` | качает ТЗ закупок, извлекает термины в снапшот — топливо «Умной сверки» |
| `site/css/styles.css` | дизайн-система (бумага/чернила/ржавчина) |
| `site/data/purchases.json` | **лента** (генерится сборщиком) · `tz.json` — термины ТЗ · `docs.json` — приложения · `spec.json` — позиции с требованиями |
| `tools/sources/split.js` | разделение снапшота на ленту и довески (см. «Доставка» ниже) |
| `tools/build_snapshot.js` | **оркестратор** — собирает все площадки, пишет снапшот |
| `tools/sources/store.js` | **накопитель активных закупок** между прогонами (см. ниже) |
| `tools/sources/eisregion.js` | регион 44-ФЗ по номеру закупки, без захода в карточку |
| `tools/sources/eissweep.js` | дата-срезы ЕИС: докуда дочитан каждый день публикации (обход потолка в 5000) |
| `tools/test_*.js` | тесты сборщика: `node --test tools/test_*.js`, без зависимостей, сеть не трогают |
| `tools/sources/portal.js` | адаптер Портала поставщиков (mos.ru) |
| `tools/sources/eis.js` | адаптер ЕИС (zakupki.gov.ru) — скрейпинг списка/карточек + обогащение 44/223 через официальный API |
| `tools/sources/eisapi.js` | **официальный интеграционный API ЕИС** (getDocsIP) через ГОСТ-TLS туннель: SOAP→ZIP→XML извещения→наша схема (см. «Официальный API ЕИС» ниже) |
| `tools/sources/util.js` | `curlAsync` + `mapLimit` (конкурентность) |
| `tools/build_analytics.js` | собирает реестр контрактов по темам → `site/data/analytics.json` (ценовой ориентир, история заказчика) |
| `tools/sources/eisContracts.js` | адаптер реестра контрактов ЕИС (для аналитики, не для ленты) |
| `tools/refresh.sh` | автообновление: пересобрать → scp на VPS (закупки — если состав изменился; аналитика — раз в сутки) |
| `tools/refresh.cmd` | обёртка для Планировщика Windows |
| `server/` | FastAPI-бэкенд (аккаунты живые; всё, что ходит в источник, — отложено) + `deploy/` |
| `server/app/spec_match.py` | мост «позиции закупки ↔ карточки товара» → движок `matcher/`, эндпоинт `POST /api/match/spec` |
| `tools/sources/ktrutable.js` | извлечение позиций из ТЗ: таблица КТРУ (с характеристиками) ИЛИ фолбэком простая товарная таблица «Наименование\|Кол-во» (реальные названия без характеристик, `pickGoodsTable`, `status:"goods"`). Раньше без КТРУ карточка показывала обрезанные основы слов — убрано (2026-08-24, п.4) |
| `matcher/` | движок сверки SpecMatch — импортирован сюда с историей (см. ниже) |

### Накопитель — почему снапшот больше не пересобирается с нуля

Раньше каждый прогон строил снапшот заново из окна ЕИС «последние обновлённые», а
**окно вращается**. Замер по двум прод-снапшотам с разницей в три дня: из 3484
закупок ЕИС, которые на вторую дату были **ещё активны**, из снапшота выпали
**3022 — 87%**. Они не истекли, они ушли из окна. Кэши при этом чистились по
составу снапшота, то есть вместе с закупкой выбрасывались её скачанные документы
и разобранные термины ТЗ. Работа каждого часа наполовину уходила в мусор, и
покрытие «Умной сверки» стояло на 38% при 24 × 220 разборов в сутки на 4149
закупок.

Теперь `tools/sources/store.js` держит закупки между прогонами:

- **выселение — только по сроку подачи** (плюс TTL для закупок без срока и общий
  потолок как страховка от роста файла);
- **слияние бережное**: свежая запись побеждает во всём, кроме дорого добытого,
  чего у неё нет (`documents`, `region`, `deliveryDays`, `tzTerms`…) — иначе прогон,
  не дошедший до карточки, стирал бы уже собранное (список — `CARRY`);
- **кэши чистятся по составу накопителя**, а не снапшота;
- **`deadlineDays`/`publishedDaysAgo` пересчитываются перед записью** — они
  считались в момент сбора, а запись живёт неделями;
- **очередь разбора ТЗ: ни разу не разобранные вперёд** — повторный разбор лишь
  обновляет версию алгоритма, а новый добавляет знание;
- **термины есть ⇒ статус `ok`** — нормализуется на выходе `annotateTz`, чтобы
  запись не могла врать о самой себе.

⚠️ **Снапшот ≠ накопитель.** В `purchases.json` уезжают ближайшие по дедлайну
`LK_SNAPSHOT_MAX` (50000, поднят 2026-08-21 — см. «Поднимать `LK_SNAPSHOT_MAX`» ниже).
Разбор ТЗ идёт по **всему** накопителю, поэтому закупка приезжает в ленту уже с
терминами, а не начинает знакомство с очереди.

### Доставка разделена: лента и довески

Снапшот был одним файлом, и браузер грузил его целиком, чтобы показать ленту.
Замер 6000 закупок (без отступов, 15.2 МБ): `tzTerms` — 23% веса, `documents` —
21%, `lots` — 12.5%. **56% байтов уходило на то, что при открытии ленты не нужно
вообще.** Отсюда и брался потолок `LK_SNAPSHOT_MAX`.

| Файл | Что | Когда грузится |
|---|---|---|
| `purchases.json` | лента | всегда |
| `tz.json` | `{id: {terms, doc}}` | только если у человека есть своё ТЗ |
| `docs.json` | `{id: [документы]}` | только при раскрытии карточки |
| `spec.json` | `{id: [позиции с характеристиками]}` | только при раскрытии карточки |

Замер на живом снапшоте: **19.6 МБ → 7.1 МБ** ленты (в 2.7 раза), круговая
проверка на 174 837 полей — 0 расхождений.

- **Пишем без отступов.** Файл читает браузер, а не человек; `JSON.stringify(x, null, 2)`
  стоил 23% байтов.
- **Вырожденные лоты выброшены.** У ЕИС адаптер синтезирует `lots: [{name: title,
  price}]` — дубль названия. Таких **5435 из 6000**; фронт восстанавливает лот из
  `title`/`price` (`normalizePurchase`). Настоящие лоты Портала (количество,
  единица, ОКПД по позиции) не трогаются — условия вырожденности нарочно строгие.
- **`docsCount`/`specCount` остаются в ленте.** Без них пустой `documents[]`
  неотличим от «довесок ещё не загружен», и карточка соврала бы «приложений нет» —
  та же причина, что у `docsFetched` в сборщике.
- **Спецификация — свой файл, а не поле внутри `tz.json`.** Добывается тем же
  разбором ТЗ, но нужна в другой момент и весит другое: замер на живом снапшоте —
  `tz.json` 4.93 МБ, из них позиции 0.23 МБ. Термины читает только «Умная сверка»
  (тумблер), позиции — **любая** раскрытая карточка, в том числе у человека без
  своего ТЗ. Слитые в один файл они заставляли бы качать пять мегабайт ради
  двенадцати строк спецификации.
- ⚠️ **Довесок документов грузится по клику, а не при отрисовке.** HTML карточки
  строится сразу для всей страницы ленты (аккордеон анимирует `grid-template-rows`,
  значит содержимое обязано быть в DOM). Запрос из `purchaseDetail` срабатывал на
  первой же ленте — проверено в браузере, `docs.json` уезжал всегда. Вызов живёт в
  обработчике раскрытия.
- ⚠️ **Раскрытая карточка обязана пережить перерисовку.** Довески приезжают в
  ответ на само раскрытие (`ensureDocs`/`ensureSpec` → `renderFeed`), поэтому
  карточка захлопывалась бы ровно тогда, когда в неё наконец приехало содержимое.
  `renderFeed` восстанавливает `is-open` по `data-id` — одним местом, а не
  вручную в каждом обработчике.
- ⚠️ **Все четыре файла обязаны быть одной версии.** Свежая лента поверх старого
  `tz.json` даст прочерки там, где термины уже добыты, а поверх старого `spec.json` —
  «загружаем спецификацию…», которое никогда не сменится. `refresh.sh` заливает все
  во временные и переименовывает одной командой; недоехавшие `.tmp` подчищает.
  Список файлов там **один** на заливку, переименование и уборку: при трёх копиях
  добавленный довесок неминуемо забылся бы в одной из них.
- ⚠️ **Отпечаток в `refresh.sh` считается по `docsCount`/`tzStatus`/`specCount`**, а
  не по `documents`/`tzTerms`/`lotItems` — их в ленте больше нет.

Поднимать `LK_SNAPSHOT_MAX` теперь можно, но **сначала замерить время разбора в
браузере**: ограничение переехало с объёма файла на разбор JSON и рендер.

⚠️ **Замер 2026-08-21 (headless-Chromium, синтетический снапшот по реальным полям):**
`renderFeed()` вызывается на **каждое** нажатие в `#search-input`/`#f-customer`
(`appview.js`, было без debounce) и гоняет `.filter()` по всему `LK.allPurchases()`.
На 6000 закупках один такой рендер уже стоил ~184 мс; на 50 000 — ~930 мс, то есть
почти секунда фриза **на каждую букву** при вводе. Поднятие `LK_SNAPSHOT_MAX` до
50000 сопровождено фиксом, без которого лента такого размера была бы неюзабельной:
  - **debounce (200 мс)** на `search-input`/`f-customer` — N нажатий подряд теперь
    дают один `renderFeed`, а не N;
  - **`basePoolCache`** — `notDone`/`passesFilters`/`passesMatch` не зависят от
    текста поиска, их результат кэшируется и не пересчитывается на каждую букву;
    сбрасывается по `poolKey()` (фильтры/`matchEnabled`/предмет ТЗ/длина ленты) и
    вручную раз в минуту (`poolCacheGen`) — «живое устаревание» (`isExpired`/
    `liveStage`) читает `Date.now()` напрямую и в `poolKey()` не входит;
  - **`searchNarrowCache`** — `passesSearch` в `tzmatch`-стиле есть конъюнкция
    префиксов, поэтому при ДОПИСЫВАНИИ запроса результат только сужается: новый
    поиск идёт не по всему пулу, а по уже найденному на предыдущую (более короткую)
    версию того же запроса.
  - Итог не бесплатный: «холодный» запрос (первый символ, или правка не по
    префиксу) на 50 000 всё ещё стоит ~930 мс — один заметный фриз после паузы в
    наборе, а не на каждую букву. Если понадобится убрать и его — кандидат:
    вынести `getBasePool`/`getSearchList` в Web Worker, не делали за неимением
    необходимости на момент замера.

Тесты: `node --test tools/test_*.js` (102 штуки, без зависимостей, сети не трогают)
и `python server/tests/test_spec_match.py` (18 штук, движок `matcher/` не мокается).

### Дата-срезы — как уйти глубже 5000 записей

Общий список ЕИС отсортирован по дате обновления, и каждый прогон читает одну и ту
же «голову». Глубже 5000 записей выдача не пускает вообще (грабли §12), поэтому
`LK_EIS_TAKE` можно поднимать сколько угодно — за пределы 5000 самых недавно
обновлённых он не выведет, а активных закупок кратно больше.

Обход — нарезка запроса по **дате публикации**: `publishDateFrom=publishDateTo=день`
даёт непересекающиеся выборки (замер: 10.08 против 11.08 — пересечение 0), и у
каждой свой потолок в 5000. Плюс разделение по закону: один день 223-ФЗ влезает
целиком (1000–2000), один день 44-ФЗ **сам упирается в потолок** (стр. 99 настоящая,
стр. 100 — кламп). Итого срез = день × закон.

`tools/sources/eissweep.js` ведёт бухгалтерию: какие срезы бывают, какой брать
следующим, докуда дочитали. Состояние — `tools/.cache/eis-sweep.json`, оно
**обязано** переживать прогоны: иначе «следующий срез» каждый раз оказывался бы
первым, и мы читали бы ту же голову, только другой формы.

- **Внутри среза сортируем по `PUBLISH_DATE`, а не `UPDATE_DATE`.** Обновления идут
  постоянно и перетасовывали бы страницы между последовательными запросами — часть
  записей мы бы прочли дважды, часть не увидели вовсе. Замер: одна и та же страница
  дважды подряд при `PUBLISH_DATE` — совпало 50/50.
- **Срезы начинаются со вчера.** Сегодняшний день ещё пополняется, и пометить его
  «вычерпан» значило бы соврать; сегодняшние публикации и так приходят общим списком.
- **Порядок обхода — вширь, потом вглубь**: сначала ни разу не тронутые срезы, среди
  них свежие дни вперёд (их закупки проживут в накопителе дольше, значит добытые для
  них документы окупятся большим числом прогонов). Дочитанный до конца срез больше не
  берётся; выпавшие из окна дни забываются.
- **Три причины остановки, и они разные по смыслу**: бюджет/страницы — «на сегодня
  хватит, вернёмся»; пустая страница — «срез вычерпан, больше не ходим»; 100-я
  страница — «дальше выдача врёт». Сетевая ошибка концом среза **не** считается:
  курсор остаётся на несработавшей странице, иначе одно моргание канала навсегда
  выкинуло бы кусок корпуса.
- ⚠️ **Приток нельзя разгонять в отрыве от `LK_EIS_DOCS`.** Закупка без документов —
  это запись со статусом «ждёт очереди»; если добывать их быстрее, чем разгребать,
  очередь растёт вечно, а покрытие сверки не двигается. Отсюда общий бюджет записей
  на прогон (`LK_EIS_SWEEP_TAKE`, 800) — он держится ниже бюджета документов.

Env: `LK_EIS_SWEEP_DAYS` (окно дней публикации, 21), `LK_EIS_SWEEP_SLICES` (срезов за
прогон, 4), `LK_EIS_SLICE_PAGES` (страниц за заход в срез, 12), `LK_EIS_SWEEP_TAKE`
(потолок новых записей со срезов за прогон, 800).

### Официальный API ЕИС — обогащение 44-ФЗ (scope A)

У ЕИС есть **официальный интеграционный API** (сервис `getDocsIP`): по реестровому
номеру он отдаёт ZIP с XML всего жизненного цикла закупки — метаданные, **контакты
44-ФЗ**, **ИНН заказчика** и список документов с **прямыми публичными URL**. Это
превосходит скрейпинг HTML-карточки: одним SOAP-вызовом вместо двух HTTP-запросов,
надёжнее и полнее (в карточке 44-ФЗ ИНН/контакты не всегда достаются). `eisapi.js`
делает SOAP→ZIP→разбор извещения (`epNotification*`) в нашу схему; документы качаются
downstream по тем же публичным `filestore/public/…file.html?uid=` (жилой IP, без
туннеля — проверено).

**Как включается.** В `collectEis` шаг 3 (обогащение) для закупок ЕИС **(44 и 223)**
сперва пробует API (`enrichViaApi`→`getDocsByReestrNumber`), при неудаче (ошибка/
noData/туннель недоступен) — **тихо откатывается на скрейпинг**, закупка не остаётся
без документов. Подсистема выбирается по закону: 44 → `PRIZ` (схема `epNotification*`),
223 → `RI223` (схема `purchaseNotice*` / `purchaseNoticeAESMBO*` и т.п.). Парсер
выбирается по корню XML (`parseNotice` → `parseNotification` для 44 /
`parseNotification223` для 223): у 223 иные теги (`inn` не `INN`, `initialSum` не
`maxPrice`, документы в `attachments/document`, контакты в `placer/contact`) и
**регион лежит прямо в XML** (`<region>`, ALL CAPS → нормализуем тем же `tidyRegion`) —
поэтому у 223 региональной регрессии нет, хотя `regionFromNumber` для 223-номеров не
работает. Документы 223 — публичный `/223/filestore/…file.html?uid=` (качаются как
44-шные, проверено). Перед прогоном воркера — один `ping()` туннеля; если stunnel не
поднят, весь шаг идёт скрейпингом. `customerInn` добавлен в `CARRY` накопителя —
добытый через API ИНН переживает прогон без туннеля.

⚠️ **Транспорт — ГОСТ-TLS туннель.** `int.zakupki.gov.ru` открывается только по
ГОСТ-TLS (браузер/обычный curl → `ERR_CONNECTION_CLOSED`, это норма). Локальный
**stunnel-msspi** (КриптоПро) принимает обычный HTTP на `127.0.0.1:1443` и заворачивает
в ГОСТ-TLS к `int.zakupki.gov.ru:443`, подставляя КЭП как клиентский сертификат.
Конфиг: `C:\Users\nikit\Downloads\stunnel.conf` (accept 127.0.0.1:1443 →
int.zakupki.gov.ru:443, лог `stunnel-eis.log`).

**Туннель оформлен автозапуском — Запланированная задача `LekaloEisTunnel`**
(при входе, от имени `nikit` Interactive, перезапуск при падении раз в минуту).
⚠️ Именно **задача от имени пользователя**, а НЕ Windows-служба: stunnel-msspi
предъявляет КЭП из **пользовательского** хранилища КриптоПро, а служба под
LocalSystem (сессия 0) его не видит — клиентский сертификат не находится.
Проверено боем: под задачей `ping()` true, API отдаёт данные. Управление:
`schtasks /Run|/End /TN LekaloEisTunnel`, `Get-ScheduledTask LekaloEisTunnel`.
Ручной запуск (для отладки): `cd C:\Users\nikit\Downloads && stunnel_msspi.exe stunnel.conf`.
Если задача не запущена (машина не в сессии пользователя) — `ping()` вернёт false и
шаг обогащения тихо уйдёт в скрейпинг. Эта же задача — пререквизит scope B (полная
замена ленты через `getDocsByOrgRegion`).

**Токен — СЕКРЕТ**, только в `.env` как `LK_EIS_TOKEN` (в git не коммитить,
`.env`/`*.env` в `.gitignore`); это токен «сервисов отдачи» (получен через
`zakupki.gov.ru/pmd/auth`, НЕ eruz). Env: `LK_EIS_API=0` — принудительно выключить
API-обогащение; `LK_EIS_TUNNEL` (127.0.0.1:1443), `LK_EIS_HOST` (int.zakupki.gov.ru).
Оба метода API (`fetchByReestr` для обогащения, `fetchByOrgRegion` для будущей замены
ленты) реализованы и покрыты тестами (`tools/test_eisapi.js`). Модуль без внешних
зависимостей: свой curl-раннер через туннель + мини-распаковщик ZIP на `zlib.inflateRaw`.

### `matcher/` — движок сверки ТЗ↔товар (импорт SpecMatch)

Отдельный проект `tender-matcher` (он же SpecMatch), влит в этот репо через
`git subtree` вместе с **96 коммитами** — до импорта его история существовала
только на одной машине, без remote. Импортирована ветка
`videolaryngoscope-bd-df-besdata`, а не `main`: `main` там отстал на 44 коммита,
вся работа за последний месяц (api, миграции, кабинет, сборные лоты) лежала на
фича-ветке.

⚠️ **История доступна, но не по пути.** `git log -- matcher/` покажет только сам
импорт: в старых коммитах пути идут от корня (`src/matcher.py`, не
`matcher/src/matcher.py`) — так работает `subtree add`. Смотреть через точку
импорта: `git log 92c3a77 -- src/matcher.py`.

| Внутри `matcher/` | Что | Статус |
|---|---|---|
| `src/matcher.py` (457 стр.) | ядро: операторы `≥/≤/range/one_of/set`, веса, дисквалификация | **живое, 39/39 тестов** |
| `src/schema.py` | `Product` · `Requirement` · `Check` · `MatchResult` | живое |
| `src/parser.py` | `.docx/.pdf/.xlsx/.xls` → текст **+ таблицы**, OCR-fallback, архивы | живое |
| `src/filter.py`, `ktru.py` | воронка: ОКПД2/КТРУ, слова, срок, регион | живое |
| `src/keymatch.py`, `extractor.py`, `embed.py` | семантика и извлечение требований | нужен `numpy` / `ANTHROPIC_API_KEY` |
| `src/tenderplan.py` | адаптер Тендерплана | **мёртвый груз** — от Тендерплана ушли |
| `api/`, `web/`, `migrations/` | свой FastAPI + кабинет + alembic | **архив-референс**, дублирует `server/` и `site/`, не деплоится |
| `tests/` | 17 файлов, включая golden | 39/39 юнит + 46/46 golden (23 фикстуры) |

Ядро **не требует зависимостей вообще** — `matcher/src` на `sys.path` и
`import matcher` работает на голом Python 3.12. Тяжёлое (`sentence-transformers`,
`easyocr` → torch, ~950 МБ venv) нужно только опциональным слоям; на VPS его не тащить.

**Стыковка с Лекало ещё не сделана.** Схемы совместимы (`verdict` совпадает
дословно, `Check.status`: у Лекало `fail` — у SpecMatch `violation`), но зазор
содержательный: `/api/match` в Лекало сверяет **текст↔текст**, а
`matcher.match()` принимает **структуру** — `Product` (карточка товара с
атрибутами; в Лекало таких карточек нет) и `List[Requirement]` (извлекаются
LLM-ом). Порядок сближения — в [`plan.md`](plan.md).

### «Умная сверка по ТЗ» — как устроена

Тумблер над лентой: человек один раз грузит своё ТЗ, и лента превращается в **поиск
по этому ТЗ** — остаются закупки про его товар, сверху самые близкие, в карточке видно,
что совпало и чего не хватает.

**Два сигнала, и они намеренно НЕ смешиваются в одно число:**

| | откуда | покрытие | что отвечает |
|---|---|---|---|
| **Предмет** | название + лоты закупки | **100%** закупок | «это вообще про мой товар?» |
| **Требования** | `tzTerms` разобранного ТЗ | ~5% и растёт | «а по параметрам проходим?» |

Слить их в один процент значило бы выдать «предмет не тот, зато шаблонные формулировки
совпали» за совпадение.

Ключевое ограничение, из которого выросла вся конструкция: **текст ТЗ закупки может
достать только сборщик.** Браузеру мешает CORS (ссылки ведут прямо на `zakupki.gov.ru`/
`zakupki.mos.ru`, скачивание работает навигацией, а прочитать содержимое можно лишь
`fetch`'ем), VPS заблокирован источниками. Поэтому:

```
сборщик (жилой IP) → качает ТЗ → термины → в снапшот (tzTerms)
                                                    ↓
                          браузер: термины моего ТЗ ∩ tzTerms → %
```

- **Термины считает один код на обе стороны** — `site/js/tzmatch.js` подключается и
  тегом `<script>`, и через `require` из сборщика (UMD-хвост в конце файла). Разъехавшийся
  стемминг давал бы разные проценты на одних и тех же данных.
- В снапшот кладём **термины, а не текст**: топ-`LK_TZ_MAX_TERMS` (120), половина —
  измеримые требования (по длине), половина — слова **по частоте в документе**.
  ⚠️ Раньше слова отбирались по длине, и это было ровно наоборот: в русском длинные
  слова канцелярские («конкурентоспособност»), а предмет короткий («перчатк»). Замер
  на живом снапшоте: предмета закупки в сохранённых терминах не оказывалось **вовсе**.
- ⚠️ **Частота без стоп-листа — та же ошибка с другого конца.** Первый живой прогон с
  частотным отбором выдал в топ `срок · заказчик · качеств · должн` — в ТЗ чаще всего
  повторяется лексика самой процедуры. Поэтому `STOP` расширен процедурными словами.
  Сверка идёт по **первым пяти буквам**, и это ограничивает список: «техническ» срезал
  бы «техника», «установленн» — «установка», «производитс» — «производительность».
  Предметные слова («материал», «размер», «объект») не трогаем вовсе: их придавит вес
  по редкости на сравнении.
- **Разбор по позициям лота** — только там, где позиций больше одной. Замер по живому
  снапшоту: таких закупок **157 из 4084 (4%), и все с Портала**; у ЕИС лот один и его имя
  в 98% случаев дословно повторяет название. Заказчик перечисляет товары внутри документа
  ТЗ, а не в структуре, а из документа мы храним 120 терминов, не текст. Зато там, где
  позиции есть, разбор важнее всего: лот «всё или ничего».
- **В интерфейсе показываем слово, а не основу** («перчатки», не «перчатк»): `surfaceForms`
  собирает самую частую исходную форму. Работает только для ТЗ пользователя — в снапшоте
  лежат одни основы, восстановить написание по ним нельзя.
- **Вариант с беглой гласной** («клинок» → «клинк») в снапшот НЕ пишем — он удваивал
  каждое слово на «-ок» и съедал места из 120. Расширяется только набор пользователя
  (`expandVariants`): пересечение множеств симметрично, одной стороны достаточно.
- ⚠️ **Метрика «доля шаблона» обманчива.** После перехода на частоту она выросла (24% →
  28%), но это не ухудшение: частотный отбор по определению берёт слова, частые и в
  других ТЗ. Мерить надо **разделение** — насколько совпадение у профильных закупок
  выше, чем у чужих. Оно выросло: +0.2 → +1.0 п.п. (пакеты) и +1.0 → +6.6 (мед. изделия).
  Низкий «шумовой пол» старых терминов достигался тем, что склейки и КТРУ не совпадали
  ни с чем — это немота, а не точность.
- Разбираем **`.docx` и `.xlsx`** (плюс файлы без расширения, опознанные по сигнатуре
  `PK`; внутри ZIP отличаем Word от Excel). `.xlsx` добавлен ради 223-ФЗ — там ТЗ часто
  Excel-лист с наименованиями и характеристиками (`xlsxTextFromBytes` в `tzmatch.js`:
  sharedStrings + листы, ячейки строки через пробел → число с единицей из соседних
  ячеек читается как требование). Спецификацию (`lotItems`) из `.xlsx` пока НЕ извлекаем
  — только термины; позиции из ячеек — отдельный шаг. `.doc`/`.xls` (старые бинарные),
  pdf, rtf, архивы честно помечаются статусом, а не считаются «по чему-то».
- ⚠️ **Кандидатов перебираем несколько, а не один.** Раньше брался ровно один документ,
  и если он оказывался не `.docx`, закупка получала `unsupported` навсегда — при том что
  рядом лежали другие файлы. Замер: перебор спасает **68%** ценой ~1.7 лишних скачиваний.
  Заведомо неразбираемые расширения (`pdf/doc/xls/rar/zip/…`) отсеиваются **до** скачивания —
  ответ и так известен. Файлы **без расширения** отсеивать нельзя: у ЕИС их половина всех
  документов (5702 из 11427), и среди них полно настоящих `.docx`.
- **Бюджет `LK_TZ_DOCS` считает СКАЧИВАНИЯ, а не закупки** — с перебором закупка стоит от
  одного до `LK_TZ_TRIES` (4) документов, а упирается всё в канал. Если бюджет кончился
  посреди перебора, вердикт — `pending`, и он НЕ кэшируется: «не разобралось» и «мы не
  дочитали» — разные вещи, а закэшированный на полпути `unsupported` закрыл бы закупке
  дорогу навсегда.
- Своё ТЗ живёт в localStorage (`lekalo_my_tz`) **терминами и их частотами, а не текстом** —
  там лимит ~5 МБ на домен, и его делят доска с поисками. Частоты нужны для предмета:
  топ основ по «часто у меня × редко в корпусе» и есть ответ на «что за товар».
- **Вес термина по редкости считает браузер** из самого снапшота (`buildCorpus`), корпус —
  `tzTerms` разобранных закупок. Без веса термин из 83% всех ТЗ значил столько же, сколько
  «нитрилов», и два НИКАК не связанных ТЗ совпадали на 16% (37% случайных пар — больше 20%).
  С весом шумовой пол падает до 6%. Корпус именно из ТЗ, **не из названий**: на корпусе
  названий редкими оказываются как раз названия товаров, «перчатк» вылетает из предмета,
  и по ТЗ на перчатки в выдачу лезут «ТОВАРЫ СПОРТИВНЫЕ» (проверено).
- `tzStatus` у закупки: `ok` · `no-doc` (нет приложений) · `pending` (не дошла очередь) ·
  `unsupported` (не .docx) · `empty` (скан) · `error`. Фронт показывает прочерк и причину,
  **процент не выдумывает**.
- Сравнение — строгое пересечение множеств. Подстрочный перебор убран: он давал
  O(терминов × моих терминов) на закупку (сотни миллионов операций на ленту) и ложные
  совпадения. Замер: 4200 сравнений — ~10 мс.

⚠️ **Единицы «г» и «в» — ловушка.** Они односимвольные и в русском совпадают с суффиксом
года («2011 г.») и предлогом («в»), поэтому в измеримые требования лезли даты и номера
реестров. Отсекаются по виду числа (`isNumericJunk`), причём **на обеих ветках разбора**:
«2011г» приходит не из числового шаблона, а из словесного — `[а-яёa-z0-9]{4,}` глотает
цифры с буквой целиком.

Env: `LK_TZ_DOCS` (бюджет СКАЧИВАНИЙ за прогон, 900; ~140 МБ трафика), `LK_TZ_TRIES` (кандидатов на закупку, 4), `LK_TZ_MAX_TERMS` (120),
`LK_TZ_CONC` (8), `LK_DOC_MAX_BYTES` (20 МБ). Кэш — `tools/.cache/tz-terms.json`,
накопительный, как и у документов ЕИС.

**Кэш ТЗ версионируется двумя метками**, и это разные вещи:

| Метка | Ключ | Что значит смена | Что выбрасывается |
|---|---|---|---|
| `TZ_ALGO` | `__algo` | изменился отбор ТЕРМИНОВ (`topTerms`/`termFreq`/`NUM_RE`) | **весь** кэш |
| `TZ_PICK` | `__pick` | изменился выбор ДОКУМЕНТА (`rankTzDocs`/`HOPELESS_EXT`/`MAX_TRIES`) | только записи со статусом ≠ `ok` |
| `TZ_ITEMS` | `__items` | изменилось извлечение ПОЗИЦИЙ (`ktrutable.js`) | только удачные разборы без `items` |

⚠️ **Метку обязана нести и сама запись, а не только файл кэша.** Прогон 14.08:
`__items` переоткрыл 1868 разборов, и 1848 из них тут же вернулись из накопителя
без спецификации — засев смотрел только на `tzAlgo`. Они застряли бы навсегда:
в кэше числятся разобранными, а позиций нет и взяться им неоткуда. Поэтому
записи метятся `tzItems`, и засев требует совпадения ОБЕИХ версий. «Разобрали
текущей версией и таблицы не нашли» (`lotItems` пуст) — это ответ, а не пробел.

Кэш хранит уже ОТОБРАННЫЕ термины, а не текст, поэтому при смене отбора старые записи
остались бы старыми навсегда. Удалить файл руками некому — сборщик на другой машине ходит
по расписанию, поэтому кэш сбрасывает себя сам. Разделение на две метки нужно потому, что
смена правила выбора документа обесценивает только неудачные вердикты: успешно разобранный
`.docx` остался успешно разобранным, и перекачивать его — впустую потраченный канал.

### Схема закупки (единая для всех площадок)
`{ id, number, title, customer, customerInn, law, source, region, okpd, price,
regionGuessed?, stage, endDate(ISO), beginDate, deadlineDays, publishedDaysAgo, guaranteeApp,
guaranteeContract, prepayment, href, deliveryDays, deliveryPlace,
contacts?{person,phone,email}, lots[{name,qty,price,okpd}], documents[{id,name,url}], matches{productId→MatchResult} }`

⚠️ **`contacts` — контакты заказчика из карточки ЕИС** (`eis.js extractContacts`,
блок «Контактная информация»; 44 — `section__title/info`, 223 — `common-text__title/value`).
Заполняется только там, где заходим в карточку (223 всегда, 44 — когда регион
неизвестен): best-effort, как регион/срок. **Портал контактов не отдаёт** (в
`Auction/Get` только имя/ИНН заказчика — проверено). Переносится между прогонами
(`store.js` CARRY), остаётся в ленте (не тяжёлый, `split.js` не выносит в довесок).
Для **ЕИС (44 и 223) контакты и ИНН теперь надёжно приходят из официального API**
(`eisapi.js` — 44: `contactPersonInfo`/`INN`, 223: `placer/contact`/`inn`), когда
туннель поднят — не зависят от того, заходили ли в карточку (см. «Официальный API
ЕИС» выше). У 223 оттуда же и регион.

---

## Источники (проверенные эндпоинты, анонимно)

**Реестр контрактов ЕИС (zakupki.gov.ru)** — источник аналитики (не активных закупок):
- список: `GET epz/contract/search/results.html?searchString=<текст>&recordsPerPage=_50&pageNumber=N`
  (те же блоки `search-registry-entry-block`: заказчик, предмет, **реальная цена контракта**, дата)
- **НЕ глушится антиботом для searchString** (в отличие от поиска активных закупок ниже) — обычный
  curl без Chromium, проверено заведомо-бессмысленным словом (честные 0). Важно: без `-L` (follow
  redirect) — с ним почему-то молча теряет searchString и отдаёт 0.
- ИНН заказчика и имя поставщика-победителя в списке **не показаны** (пустой `inn=` в ссылке на
  заказчика) — победитель только в карточке `contractCard/...`, отдельный заход, не собираем.
- Число участников/уровень конкуренции — не нашли нигде в списке, вероятно только в протоколе
  подведения итогов конкретной закупки; **не реализовано**, честно написано в UI, а не выдумано.

**Портал поставщиков (mos.ru)** — чистый JSON:
- поиск: `GET old.zakupki.mos.ru/api/Cssp/Purchase/Query?queryDto=<json>` (typeIn=1 КС, stateIdIn=19000002 «Активная»)
- карточка: `GET zakupki.mos.ru/newapi/api/Auction/Get?auctionId=<id>` (files[], items[], deliveries[])
- файл: `GET zakupki.mos.ru/newapi/api/FileStorage/Download?id=<fileId>`

**ЕИС (zakupki.gov.ru)** — HTML/RSS:
- список: `GET epz/order/extendedsearch/results.html?fz44=on&fz223=on&af=on&recordsPerPage=_50&pageNumber=N`
  (в блоках `search-registry-entry-block`: №, закон, предмет, заказчик, цена, срок)
- карточка (для вкладки документов): открыть `href` закупки со следованием 301
- документы: со страницы `…/documents.html` — ссылки `…/filestore/…/file.html?uid=…`
- **документов в списковой выдаче нет.** У **44-ФЗ** (68% закупок ЕИС) вкладка открывается
  ПРЯМО: тот же адрес, `common-info.html` → `documents.html`, **один запрос вместо двух**
  (проверено на 15 закупках: 14 отдали то же самое, одна — больше). У **223-ФЗ** так нельзя —
  их `documents.html` требует `noticeGuid` из карточки, без него 301 → 404.
- **регион** есть только в карточке, но у 44-ФЗ он выводится из номера (см. `eisregion.js`),
  поэтому в карточку идут лишь закупки с незнакомым кодом — они же справочник и пополняют.
- отсюда бюджет `LK_EIS_DOCS` и накопительный кэш (см. «Как обновить данные»),
  а не «качаем всем подряд».
- ⚠️ **выдача обрывается на 5000 записей** — см. «Грабли» §12. Обход — **дата-срезы**
  (ниже).

**Скачивание файлов работает из браузера напрямую** (навигация, не fetch → CORS не мешает;
браузер пользователя с жилым IP видит источник). Сервер к источникам не ходит.

---

## Как обновить данные

Сбор — **на любой машине с доступом к mos.ru/zakupki.gov.ru** (но не на VPS!). Проверено
2026-08-10: копия (`C:\Users\user\TenderPlan-COPY`) собирает штатно. Что нужно на новой
машине: Node (переносимый zip с nodejs.org годится, ставить в систему не обязательно) и
`playwright-core` в `tools/` — полный `playwright` тянет свой chromium на ~150 МБ, а хватает
системного Chrome через `LK_CHROME_PATH`. **Кэши машинно-специфичны** (`tools/.cache/`,
gitignore): холодный прогон на второй машине выдаст снапшот беднее по документам, чем
накопленный на первой. Кэш карточек ЕИС восстанавливается из прод-снапшота (в нём есть
`documents`/`region`/`deliveryDays`) — так и сделали, 598 записей.

Обычный порядок:
```bash
node tools/build_snapshot.js         # пишет site/data/purchases.json (~5-6 мин)
scp -i ~/.ssh/nexara_deploy site/data/purchases.json root@147.45.141.237:/opt/lekalo/site/data/purchases.json.tmp
ssh -i ~/.ssh/nexara_deploy root@147.45.141.237 'mv /opt/lekalo/site/data/purchases.json{.tmp,}'
```
Или разом: `bash tools/refresh.sh` (доставляет по scp только если изменился состав).

**Снапшот больше НЕ коммитится в git** (см. «Грабли» — на ~4000+ закупках это ~7 МБ,
раздуло бы историю). `.gitignore` его исключает; на диске файл остаётся как обычно,
просто git его не отслеживает. Код (`site/js`, `server/`, `tools/`) — по-прежнему
через обычный `git push` + `ssh … git pull` на VPS, вручную.

**Автообновление:** Планировщик Windows `LekaloSnapshotRefresh` запускает `refresh.cmd` каждый
час (лог `C:\Users\user\lekalo-refresh.log`). Управление: `schtasks /Run|/Change /TN LekaloSnapshotRefresh`.
Проверено: интервал ровно `PT1H`, без пропусков.

**`refresh.sh` сам делает `git pull --ff-only`** перед сбором. Это важно для работы с двух
машин: правки пушатся с копии, а сборщик крутится на основной по расписанию — раньше он
всегда исполнял код с последнего РУЧНОГО пулла, и исправления в самом сборщике до него не
доезжали вовсе, сколько их ни пушь. Неудачный пулл не роняет прогон (данные важнее
синхронизации кода), но и обратная сторона есть: сломанный запушенный сборщик остановит
ежечасный сбор. Прод при этом не ломается — на VPS остаётся прежний снапшот, он просто
стареет.

**Документы ЕИС копятся между прогонами.** `LK_EIS_DOCS` — это бюджет заходов в карточки
**за один прогон**, а не потолок покрытия: добытое ложится в `tools/.cache/eis-docs.json`
(gitignore, не деплоится) и переживает пересборку. Бюджет тратится по приоритету —
(1) тематические из keyword-проходов, которых ещё нет в кэше, (2) прочие новые по близости
дедлайна, (3) остатком — обновление записей старше `LK_EIS_DOCS_TTL_DAYS`. При ежечасном
расписании покрытие накапливается за сутки, длительность прогона не растёт. Кэш чистится
от закупок, ушедших из снапшота; при пустом ответе ЕИС не трогается (чтобы сбой источника
не стёр накопленное). `refresh.sh` сверяет отпечаток `id:естьЛиДокументы` — иначе прогон,
добывший ТЗ без смены состава, не доехал бы до прода.

**Тюнинг объёма (env):** `LK_SNAPSHOT_TAKE` (Портал, деф. 500), `LK_EIS_TAKE` (ЕИС общий список, 2000
— ⚠️ выше 5000 поднимать бессмысленно, см. дата-срезы),
`LK_EIS_DOCS` (бюджет карточек ЕИС за прогон, 900), `LK_EIS_DOCS_TTL_DAYS` (когда перепроверять
кэш документов, 7), `LK_EIS_KEYWORDS`
(свой список тем через запятую вместо ~30 по умолчанию), `LK_EIS_KEYWORD_TAKE` (закупок на тему, 60),
`LK_POOL`, `LK_PORTAL_CONC`, `LK_EIS_CONC`. Прицельный поиск по темам ЕИС идёт через headless-Chromium
(Playwright), не curl — источник глушит поиск для голых HTTP-клиентов (см. «Грабли»).

**Накопитель (env):** `LK_SNAPSHOT_MAX` (сколько закупок уезжает в `purchases.json`, 50000 —
поднят 2026-08-21 с 6000, см. замер выше — ограничение доставки, не сбора), `LK_STORE_TTL_DAYS` (когда выселять закупку без срока подачи, 30),
`LK_STORE_MAX` (потолок файла накопителя, 80000 — поднят 2026-08-21: прогон упирался ровно в
прежний потолок 40000, значит реального предложения больше). ⚠️ Не путать `LK_POOL` (размер запроса списка
Портала) с накопителем — это разные вещи.

---

## VPS (Timeweb) — деплой

- IP `147.45.141.237`, ключ `C:/Users/user/.ssh/nexara_deploy`, `root`. nginx отдаёт
  `/opt/lekalo/site` (это git-клон репо), сайт `lekalo` включён.
- Сервер поднят с нуля 18.08.2026 на новом аккаунте Timeweb. Прежний (`186.246.30.213`,
  там же лежала статика Nexara) утрачен: пароль панели изменён, машина приостановлена за
  неуплату. Переехали чистым стартом — клиентов на ней не было, кроме своего тестового
  аккаунта, поэтому `lekalo.db` создан заново, а секреты сгенерированы на месте.
- ⚠️ **`104.171.137.131` — записанный тогда же IP оказался неверным** (сервер по нему
  не отвечал вовсе — не SSH-бан, а именно не тот адрес). Реальный IP этой же машины —
  `147.45.141.237`; сам сервер (код, БД, systemd-юнит) не переустанавливался, поменялся
  только адрес в доке. Если где-то в старых заметках/памяти встретится `104.171.137.131` —
  это опечатка того переезда, актуальный IP — текущий, из строки выше.
- Деплой правок = `git push` + `ssh … 'cd /opt/lekalo && git pull --ff-only'`.
- Живой сайт: **`https://147.45.141.237.nip.io/`** (HTTPS, Let's Encrypt через nip.io — см.
  `server/deploy/enable-https.sh`). Голый IP `http://147.45.141.237/` тоже работает для
  ленты/админки (старые ссылки не ломаются), но вход/регистрация/кабинет с него редиректят
  на HTTPS-домен — secure-cookie сессии по голому HTTP браузер не сохранит.

---

## Грабли (проверено болью)

1. **VPS заблокирован источниками.** С дата-центра `mos.ru` и `zakupki.gov.ru` не открываются
   (TCP-таймаут), хотя `ya.ru`/интернет есть. Поэтому сбор — на машине пользователя, а не на сервере.
   Из-за этого live-бэкенд на этом VPS невозможен.
2. **Локальный `python` — сломанная Store-заглушка** (Windows). Печатает «Python», ничего не
   исполняет. Для инструментов — Node. FastAPI запускать только на хосте с доступом к источникам.
3. **SSH host-key на VPS меняется после ребута** (Timeweb пересоздаёт окружение). При `REMOTE HOST IDENTIFICATION
   CHANGED` → `ssh-keygen -R 147.45.141.237` и переподключиться (это НЕ fail2ban).
4. **fail2ban на VPS:** если SSH не подключился с первой попытки (таймаут «banner exchange») —
   НЕ долбить повторно, попросить пользователя перезагрузить сервер через панель Timeweb.
5. **Cyrillic в curl-параметрах** → кракозябры. Кодировать через `curl --data-urlencode` / `--data`,
   не вставлять кириллицу прямо в URL.
6. **Снапшот в git больше не коммитится** (было 2.1 МБ ежечасно, на ~4000+ закупках выросло бы
   до ~7 МБ и раздуло историю) — доставляется на VPS напрямую по `scp` (`tools/refresh.sh`),
   `.gitignore` файл исключает. На диске он есть как обычно, просто не отслеживается.
7. **`sites-enabled/lekalo` может незаметно перестать быть симлинком** на
   `sites-available/lekalo` (разово случилось 2026-07-28, вероятно из-за ручной правки
   или certbot) — тогда правки конфига (например, `enable-ratelimit.sh`) уходят в файл,
   который nginx НЕ грузит: `nginx -t`/`reload` проходят успешно, но поведение не
   меняется (лимит стоял в конфиге, а 200 быстрых запросов подряд ни разу не поймали
   429). После любой правки nginx-конфига проверять: `ls -la /etc/nginx/sites-enabled/lekalo`
   должен показывать `-> /etc/nginx/sites-available/lekalo` (симлинк), а не `-rw-r--r--`
   (независимый файл). `enable-ratelimit.sh` теперь сам чинит симлинк при расхождении.
8. **Поиск по ключевым словам (searchString) ЕИС глушится для «голых» HTTP-клиентов** —
   curl с этой машины получает «0 записей» на любой поисковый запрос (проверено заведомо
   новыми словами), а тот же запрос с той же машины/IP через headless-Chromium (Playwright) —
   реальные результаты. Не IP-бан, а разбор клиента (TLS/JS-отпечаток). Поэтому
   `collectEis`'s прицельный поиск по темам идёт через Playwright, а не curl (общий список
   без поиска — по-прежнему curl, там не глушит). Не пытаться обходить проксями/спуфингом.
9. **`\b` и `\w` в JS не видят кириллицу — регэксп молча не срабатывает.** В JS `\w` это
   ровно `[A-Za-z0-9_]`, поэтому после «мм» границы слова `\b` не возникает НИКОГДА.
   В `tzmatch.js` из-за этого `NUM_RE` не извлекал числовые требования (`0,1 мм`, `500 шт`)
   с самого написания файла — при том что дословно тот же шаблон в Python-порте
   (`server/app/matching.py`) работает, там `\b` юникодный. Ловится только прогоном на
   настоящем тексте, глазами — нет. Правило: в JS по кириллице только явные классы
   (`[а-яё]`) и просмотры вперёд `(?![а-яё])`, никогда `\b`/`\w`. Та же грабля раньше
   стоила цикла сборки на сроках ЕИС («рабочих» не ловилось `\w*`).
10. **Выдача ЕИС — вращающееся окно, а не список активных закупок.** `sortBy=UPDATE_DATE`
    отдаёт последние обновлённые, и закупка уходит из выдачи, оставаясь активной: за три
    дня из снапшота выпали 87% ещё живых закупок ЕИС. Пока состав снапшота задавался
    окном, а кэши чистились по составу снапшота, скачанные ТЗ выбрасывались и качались
    заново — покрытие сверки упиралось в потолок, никак не связанный с бюджетами. Отсюда
    накопитель (см. выше). **Правило: любое множество «что мы знаем» строить по
    накопителю, а не по тому, что пришло в этом прогоне.**
11. **Статус и содержимое записи умеют разъезжаться.** Запись живёт в накопителе неделями,
    и код, который её помечает, видит не только свежесобранное. Прогон успел записать
    «ждёт очереди» полутора тысячам уже разобранных закупок — фронт не сломался лишь
    потому, что смотрит на `tzTerms` раньше, чем на `tzStatus` (`appview.js:437`).
    Инварианты вроде «термины есть ⇒ статус `ok`» чинить одним местом на выходе, а не
    в каждой ветке.
12. **Выдача ЕИС обрывается ровно на 5000 записях, и обрыв МОЛЧАЛИВЫЙ.** Страница 100
    (при `recordsPerPage=_50`) и любая дальше отдают честные 50 блоков — но это клон
    последней настоящей страницы. Проверять глубину числом блоков бессмысленно: `pageNumber=1000`
    выглядит рабочим. Мерить только **уникальностью номеров** (шумовой пол: та же страница
    дважды подряд даёт 50/50 совпадений, так что пересечение = настоящий повтор).
    Замер: стр. 99 (4950) — настоящая, стр. 100 — уже кламп.
    Обход **реализован** — дата-срезы (см. раздел выше): `publishDateFrom`/`publishDateTo`
    дают непересекающиеся выборки (11.08 против 12.08 — пересечение 0), у каждой свой
    потолок. Один день 223-ФЗ влезает целиком (1000–2000), один день 44-ФЗ сам упирается
    в 5000 — **полноты по дню 44-ФЗ у нас нет**, для неё нужен третий срез (цена/регион).
    Сейчас это не мешает: узкое место — добыча документов (`LK_EIS_DOCS`), а не глубина
    выдачи, и брать больше, чем успеваем разобрать, смысла нет.
13. **Один документ на закупку — этого мало.** `pickTzDoc` берёт лучший по имени, и если он
    не .docx, закупка получает `unsupported` навсегда. Замер на 40 закупках: перебор
    следующих кандидатов спасает **68%** ценой ~1.7 лишних скачиваний. Имена не помогают —
    у ЕИС половина документов вообще без расширения, решает только сигнатура `PK`.

---

## Git

Ветка `main`. **Не коммитить:** секреты (`.env`, токены), `certs/`, `scratchpad/`, `server/.venv/`
(см. `.gitignore`). Коммит-сообщения заканчивать:
`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
Пуш/деплой данных — часть рабочего цикла обновления (см. выше); правки кода — по договорённости.
